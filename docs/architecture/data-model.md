---
generated: true
last_updated: 2026-04-24T00:00:00Z
source: session-setup
project: anchor-management-tools
---

# Data Model

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

See also: [[overview]], [[server-actions]], [[relationships]]

## Note

Full live schema is maintained by the database agent and available via:

```sql
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema = 'public'
ORDER BY table_name, ordinal_position;
```

This file will be populated with the full schema on next database-agent refresh.

## Known Tables (from codebase scan)

Discovered from `.from('table_name')` references across server actions and pages:

### Employees & HR

| Table | Purpose |
|-------|---------|
| `employees` | Staff records |
| `employee_pay_settings` | Pay rates and settings per employee |
| `leave_requests` | Holiday/leave requests |
| `rota_weeks` | Weekly rota containers |
| `rota_shifts` | Individual shifts within a rota week |
| `timeclock_sessions` | Clock-in/out records |
| `payroll_periods` | Payroll run periods |
| `mileage_claims` | HMRC mileage expense claims |
| `expenses` | General expense records |

### Customers & Bookings

| Table | Purpose |
|-------|---------|
| `customers` | Customer CRM records |
| `bookings` | Event bookings |
| `guest_tokens` | One-time guest access tokens |
| `table_bookings` | Table/Sunday lunch bookings |
| `sunday_preorders` | Pre-orders attached to table bookings |
| `table_booking_waitlist` | Waitlist entries |
| `private_bookings` | Private event bookings |
| `private_booking_payments` | Payment records for private bookings |
| `private_booking_vendors` | Vendor options for private bookings |
| `private_booking_spaces` | Venue space definitions |

### Events

| Table | Purpose |
|-------|---------|
| `events` | Event records |
| `event_tickets` | Ticket types per event |

### Parking

| Table | Purpose |
|-------|---------|
| `parking_bookings` | Parking space reservations |

### Messaging

| Table | Purpose |
|-------|---------|
| `sms_messages` | Individual SMS records |
| `sms_threads` | SMS conversation threads |
| `bulk_sms_jobs` | Bulk SMS campaign jobs |

### Finance

| Table | Purpose |
|-------|---------|
| `invoices` | Invoice records |
| `invoice_items` | Line items on invoices |
| `receipts` | Receipt records |
| `receipt_transactions` | Individual transactions on receipts |
| `attachment_categories` | Categories for receipt attachments |
| `budgets` | Budget tracking records |

### System

| Table | Purpose |
|-------|---------|
| `audit_logs` | Immutable audit trail for all mutations |
| `background_jobs` | Async job queue |
| `api_keys` | External API key management |
| `user_roles` | RBAC role assignments |
| `roles` | Role definitions |
| `permissions` | Permission definitions per role |
| `calendar_notes` | Staff calendar annotations |
