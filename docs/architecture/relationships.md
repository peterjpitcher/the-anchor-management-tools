---
generated: true
last_updated: 2026-04-24T00:00:00Z
source: session-setup
project: anchor-management-tools
---

# Relationships

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

See also: [[overview]], [[routes]], [[server-actions]], [[data-model]]

Cross-reference map linking tables → actions → routes → integrations.

---

## Customers

```
customers
  ← actions/customers.ts (CRUD, merge)
  ← actions/eventBookings.ts (booking.customer_id)
  ← actions/tableBookings.ts (booking.customer_id)
  ← actions/privateBookingActions.ts (booking.customer_id)
  → /customers (list)
  → /customers/[id] (detail)
  → /customers/insights (analytics)
```

---

## Events & Bookings

```
events
  ← actions/events.ts (CRUD, publish)
  → /events/* (management UI)
  → Stripe (ticket payments via /api/webhooks/stripe)
  → PayPal (ticket payments via /api/webhooks/paypal)
  → Twilio (SMS reminders via cron)
  → Microsoft Graph (email confirmations)
  → QRCode (ticket QR generation in event-marketing.ts)

bookings
  ← actions/eventBookings.ts
  → /g/[token]/manage-booking/action (guest amendment/cancel)
  → /g/[token]/event-payment/checkout (payment)
  → guest_tokens (one-per-booking access link)

guest_tokens
  ← actions/eventBookings.ts
  → /r/[token] (short-link redirect)
  → /g/[token]/* (guest flow entry point)

event_tickets
  ← actions/eventTickets.ts
  → events (many ticket types per event)
```

---

## Private Bookings

```
private_bookings
  ← actions/privateBookingActions.ts
  → /private-bookings/* (management UI)
  → /booking-portal/[token] (customer portal)
  → private_booking_payments (deposit tracking)
  → PayPal (/api/webhooks/paypal/private-bookings)
  → Twilio (SMS queue via sms_queue / private-bookings/sms-queue)
  → Microsoft Graph (contract emails)

private_booking_spaces
  ← actions/privateBookingSpaces.ts
  → /private-bookings/settings/spaces

private_booking_vendors
  ← actions/privateBookingVendors.ts
  → /private-bookings/settings/vendors
```

---

## Table Bookings (Sunday Lunch)

```
table_bookings
  ← actions/tableBookings.ts
  → /g/[token]/table-manage/action (guest management)
  → /g/[token]/sunday-preorder/action (pre-orders)
  → sunday_preorders (pre-order line items)
  → table_booking_waitlist (overflow)
  → PayPal (/api/external/table-bookings/[id]/paypal/*, /api/webhooks/paypal/table-bookings)
  → Microsoft Graph (confirmation emails)
  → Twilio (reminder SMS)

table_booking_waitlist
  ← actions/tableBookingWaitlist.ts
  → /g/[token]/waitlist-offer/confirm (offer acceptance)
```

---

## Employees & HR

```
employees
  ← actions/employees.ts
  → /employees/* (management UI)
  → /portal/shifts (staff self-service)
  → employee_pay_settings (pay rates)
  → timeclock_sessions (clock records)
  → rota_shifts (scheduled shifts)
  → leave_requests (absence management)
  → payroll_periods (pay runs)
  → Microsoft Graph (onboarding/offboarding emails)

rota_weeks + rota_shifts
  ← actions/rota.ts
  → /rota/* (management UI)
  → /portal/shifts (staff view)
  → Vercel cron: /api/cron/rota-auto-close, rota-manager-alert, rota-staff-email

timeclock_sessions
  ← actions/timeclock.ts
  → /timeclock (public kiosk)
  → /portal/shifts (staff view of hours)

payroll_periods
  ← actions/payroll.ts
  → payroll UI
  → expenses + mileage_claims (deductions)
  → Microsoft Graph (payslip emails to PAYROLL_ACCOUNTANT_EMAIL)
```

---

## Parking

```
parking_bookings
  ← actions/parking.ts
  → /api/parking/bookings (external API)
  → /api/parking/payment/return (PayPal return)
  → /api/webhooks/paypal/parking
  → Vercel cron: /api/cron/parking-notifications
  → Twilio (parking SMS notifications)
```

---

## Messaging

```
sms_messages + sms_threads
  ← actions/messages.ts
  → /messages/* (inbox UI)
  → Twilio (/api/webhooks/twilio inbound; lib/twilio.ts outbound)
  → Vercel cron: /api/cron/reconcile-sms

bulk_sms_jobs
  ← actions/bulk-messages.ts
  → /messages/bulk (campaign UI)
  → Twilio (batch send)
```

---

## Finance

```
invoices + invoice_items
  ← actions/invoices.ts
  → /invoices/* (management UI)
  → Microsoft Graph (invoice email delivery)

receipts + receipt_transactions
  ← actions/receipts.ts
  → /receipts/* (management UI)
  → OpenAI (AI parsing in actions/ai-menu-parsing.ts, actions/receipts.ts)

budgets
  ← actions/budgets.ts
  → /cashing-up/* (daily/weekly financial management)
```

---

## System

```
audit_logs
  ← actions/audit.ts (logAuditEvent — called by all mutation actions)
  → /audit-logs/* (admin view)

background_jobs
  ← actions/backgroundJobs.ts
  → Job queue for async SMS, email, payment tasks

api_keys
  ← (authenticated)/settings/api-keys/actions.ts
  → External API authentication (/api/external/*, /api/parking/*)

user_roles + roles + permissions
  ← actions/rbac.ts (checkUserPermission, getUserPermissions)
  → (authenticated)/layout.tsx (gate at layout level)
  → Every authenticated page and server action
```

---

## Integration Summary

| Integration | Triggers from | Receives events via |
|-------------|--------------|---------------------|
| Twilio SMS | `src/lib/twilio.ts`, cron jobs, action files | `/api/webhooks/twilio` |
| Microsoft Graph email | `src/lib/email/emailService.ts` | n/a (send-only) |
| Stripe | `actions/eventBookings.ts`, checkout routes | `/api/webhooks/stripe` |
| PayPal | `src/app/api/external/table-bookings/`, guest checkout routes | `/api/webhooks/paypal/*` |
| OpenAI | `actions/ai-menu-parsing.ts`, `actions/receipts.ts` | n/a (send-only) |
| Google Calendar | Optional — `src/lib/google/` | n/a |
| Cloudflare Turnstile | Public booking forms (client-side widget) | Server-side token verify |
