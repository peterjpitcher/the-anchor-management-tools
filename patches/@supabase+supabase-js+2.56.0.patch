diff --git a/node_modules/@supabase/supabase-js/dist/module/index.js b/node_modules/@supabase/supabase-js/dist/module/index.js
index 5197a91..08f4f98 100644
--- a/node_modules/@supabase/supabase-js/dist/module/index.js
+++ b/node_modules/@supabase/supabase-js/dist/module/index.js
@@ -17,11 +17,12 @@ function shouldShowDeprecationWarning() {
         return false;
     }
     // Skip if process is not available (e.g., Edge Runtime)
-    if (typeof process === 'undefined') {
+    const processGlobal = typeof globalThis !== 'undefined' ? globalThis['process'] : undefined;
+    if (!processGlobal) {
         return false;
     }
     // Use dynamic property access to avoid Next.js Edge Runtime static analysis warnings
-    const processVersion = process['version'];
+    const processVersion = processGlobal['version'];
     if (processVersion === undefined || processVersion === null) {
         return false;
     }
