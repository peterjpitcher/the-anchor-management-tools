# Supabase Configuration
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application Configuration
# For local development: http://localhost:3000
# For production: https://management.orangejelly.co.uk
NEXT_PUBLIC_APP_URL=https://management.orangejelly.co.uk
NEXT_PUBLIC_CONTACT_PHONE_NUMBER=+1234567890

# Twilio Configuration (Optional - for SMS functionality)
# Leave empty if SMS features are not needed
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Twilio Webhook Configuration (Optional)
# Set to 'true' to skip signature validation for testing (NOT recommended for production)
# SKIP_TWILIO_SIGNATURE_VALIDATION=false

# Security
# Generate a random string for cron job authentication
# Used by Vercel to authenticate cron job requests
CRON_SECRET=your-secret-key-here

# GitHub Bug Reporter (Optional - but required for bug reporting feature)
# Without this token, users will see "Bug reporting is not configured" error
# 
# To generate a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token" → "Generate new token (classic)"
# 3. Name it (e.g., "Anchor Management Bug Reporter")
# 4. Select scope: 'public_repo' (for public repos) or 'repo' (for private repos)
# 5. Generate and copy the token immediately
# 
# Format: ghp_xxxxxxxxxxxxxxxxxxxx
GITHUB_BUG_REPORTER_TOKEN=
GITHUB_OWNER=peterjpitcher
GITHUB_REPO=the-anchor-management-tools

# Microsoft Graph Email Configuration (Optional - for invoice/quote email sending)
# Required for sending invoices and quotes via email
# 
# To set up:
# 1. Go to Azure Portal (https://portal.azure.com)
# 2. Navigate to Azure Active Directory > App registrations
# 3. Create new registration or use existing
# 4. Note the Application (client) ID and Directory (tenant) ID
# 5. Under Certificates & secrets, create a new client secret
# 6. Under API permissions, add Microsoft Graph > Application permissions: Mail.Send
# 7. Grant admin consent for the permissions
#
MICROSOFT_TENANT_ID=23209e42-1439-448a-a1e1-465855aa2572
MICROSOFT_CLIENT_ID=915467b4-9e59-4433-a02b-f443567716ca
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here
MICROSOFT_USER_EMAIL=peter@orangejelly.co.uk
MICROSOFT_VALUE=your_microsoft_value_here

# Google Calendar Configuration (Optional - for calendar sync)
# Service account key as single-line JSON (use scripts/fix-google-service-key.ts to convert)
GOOGLE_SERVICE_ACCOUNT_KEY=
# Google Calendar ID to sync bookings to
GOOGLE_CALENDAR_ID=