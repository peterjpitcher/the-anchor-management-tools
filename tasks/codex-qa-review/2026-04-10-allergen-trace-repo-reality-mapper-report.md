Current repo state: `option_group` is already implemented end to end for dish ingredients/recipes and for max-per-group worst-case costing. None of the spec’s new fields exist yet: `inclusion_type`, `upgrade_price`, `removable_allergens`, and `is_modifiable_for`.

1. Schema state. In [20260519000000_add_option_groups.sql](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20260519000000_add_option_groups.sql:10), the only schema columns added are `menu_dish_ingredients.option_group TEXT` and `menu_dish_recipes.option_group TEXT`. The current `menu_refresh_dish_calculations` already handles `option_group` for costing: ungrouped rows are summed, grouped rows use `MAX(line_cost)` per `option_group`, then those maxes are summed. It does not know about `inclusion_type`, `upgrade_price`, removable allergens, or upgrade exclusion.

2. Current cost helpers. In [DishCompositionTab.tsx](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishCompositionTab.tsx:19), `CostBreakdown` is `{ total, fixedTotal, groups }` where `groups` is `Map<string, { maxCost: number; items: Array<{ name: string; cost: number }> }>` . `computeIngredientCost` and `computeRecipeCost` already handle `option_group`: blank group means add to `fixedTotal`; populated group means track each line in that group and use the group’s max cost in `total`. They return worst-case grouped costing, not inclusion/removable/upgrade splits.

3. Current form types. In [CompositionRow.tsx](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/CompositionRow.tsx:76), `DishIngredientFormRow` has `ingredient_id`, `quantity`, `unit`, `yield_pct`, `wastage_pct`, `cost_override`, `notes`, `option_group`. `DishRecipeFormRow` has `recipe_id`, `quantity`, `yield_pct`, `wastage_pct`, `cost_override`, `notes`, `option_group`. `option_group` is already present; `inclusion_type` and `upgrade_price` are not.

4. Current drawer. In [DishDrawer.tsx](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishDrawer.tsx:191), hydration maps `detail.ingredients` and `detail.recipes` into form rows and sets `option_group: (row.option_group as string) || ''`. On save, it trims and sends `option_group: row.option_group?.trim() || undefined` for both ingredients and recipes ([DishDrawer.tsx](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishDrawer.tsx:304)); `MenuService` then normalizes that to `null` before calling the RPC ([menu.ts](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/services/menu.ts:1108)). `tabItems` is a plain array, not memoized, with four tabs: `overview`, `composition`, `menus`, and `gp-analysis` ([DishDrawer.tsx](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishDrawer.tsx:386)).

5. Current GP Analysis tab. In [DishGpAnalysisTab.tsx](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishGpAnalysisTab.tsx:63), it computes combination analysis for all current option groups: fixed cost from ungrouped rows, merged groups across ingredients and recipes by shared group name, cartesian product of one item per group, then portion cost, GP%, below-target status, and target sell price per combination. It sorts worst cost first and trims to worst 20 + best 20 if combinations exceed 100. It does not compute upgrade impact or any allergen summary.

6. Zod schemas. In [menu.ts](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/services/menu.ts:42), both `DishIngredientSchema` and `DishRecipeSchema` already include `option_group: z.string().nullable().optional()`. Neither includes `inclusion_type` or `upgrade_price`.

7. `menu_dishes` columns. I confirmed against the live Supabase table configured in the repo. Current columns are: `id`, `name`, `slug`, `description`, `selling_price`, `target_gp_pct`, `portion_cost`, `gp_pct`, `allergen_flags`, `dietary_flags`, `calories`, `is_active`, `is_sunday_lunch`, `image_url`, `notes`, `is_gp_alert`, `created_at`, `updated_at`. It does not have `removable_allergens` or `is_modifiable_for`; live queries for those columns fail.

8. Actual allergen data on `menu_ingredients`. Schema/storage format is `TEXT[] NOT NULL DEFAULT '{}'` ([20251123120000_squashed.sql](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20251123120000_squashed.sql:16338)); in app code it is handled as `string[]` ([menu.ts](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/services/menu.ts:27)). I queried the live `menu_ingredients` table: the distinct allergen values currently in use are `celery`, `crustaceans`, `eggs`, `fish`, `gluten`, `milk`, `mustard`, `nuts`, `sesame`, `soya`, `sulphites`. The code’s canonical 14-allergen list in [ai-menu-parsing.ts](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/actions/ai-menu-parsing.ts:41) is `celery`, `gluten`, `crustaceans`, `eggs`, `fish`, `lupin`, `milk`, `molluscs`, `mustard`, `nuts`, `peanuts`, `sesame`, `soya`, `sulphites`, so the live ingredient data currently does not use `lupin`, `molluscs`, or `peanuts`. Values are stored as lowercase slug strings inside arrays, e.g. `["gluten","milk","soya","sulphites"]`.

9. Existing allergen aggregation. In [20260519000000_add_option_groups.sql](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20260519000000_add_option_groups.sql:113), `allergen_flags` and `dietary_flags` are computed by `UNION ALL`-ing all ingredient rows and recipe rows into `all_flags`, then `UNNEST` + `SELECT DISTINCT TRIM(flag)` over each array. Ingredient rows contribute `menu_ingredients.allergens` and `menu_ingredients.dietary_flags`; recipe rows contribute `menu_recipes.allergen_flags` and `menu_recipes.dietary_flags`. This includes grouped alternatives too; `option_group` affects cost only, not allergen/dietary aggregation. There is no removable/choice-aware logic, no upgrade exclusion, and `dietary_flags` are currently a plain union of row-level flags, not a whole-dish compatibility calculation.