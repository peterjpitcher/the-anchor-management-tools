The spec’s 3-bug cascade is not supported as written. The code confirms a real URL/body-sensitive idempotency conflict, but 17 actual resends require at least one more runtime condition the spec does not prove.

1. Claim 1 — `PARTIALLY CONFIRMED`.
The mechanics are real: `processTableReviewFollowups()` creates a random guest token before `sendSMS` at [route.ts:1151](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1151), and `createGuestToken()` uses `crypto.randomBytes` at [tokens.ts:24](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/guest/tokens.ts:24). `buildSmsDedupContext()` includes `body` in `requestHash` at [safety.ts:144](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/sms/safety.ts:144), so a new URL changes the hash. `idempotency_conflict` is fatal: `sendSMS()` returns it at [twilio.ts:304](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/twilio.ts:304), the cron records it as fatal at [route.ts:144](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:144), and then aborts the whole run at [route.ts:1202](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1202) and [route.ts:1685](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1685).
What is not confirmed is “PRIMARY CAUSE”. On later runs, `loadSentTableTemplateSet()` and `sentSet.has(...)` run before token creation at [route.ts:1092](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1092) and [route.ts:1130](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1130). If message logging or `review_sms_sent_at` worked, reruns would never reach token creation.

2. Claim 2 — `DISPROVED`.
The default idempotency TTL is 14 days, not minutes: [safety.ts:82](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/sms/safety.ts:82). The env parser only accepts positive integers, so the minimum override is 1 hour at [safety.ts:59](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/sms/safety.ts:59), still too long for 15-minute reruns. The reclaim path does work as described at [safety.ts:254](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/sms/safety.ts:254), but the repo does not show any caller for `cleanup_expired_idempotency_keys()`; it only defines the function at [20251123120000_squashed.sql:11116](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20251123120000_squashed.sql:11116). So “expires between 15-minute runs” is not supported by code defaults.

3. Claim 3 — `PARTIALLY CONFIRMED`.
Silent post-send failure absolutely can make the booking eligible again. The update only warns on error or zero rows at [route.ts:1211](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1211), while the loader only fetches `status='confirmed'` rows at [route.ts:653](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:653), and the eligibility filter also requires `!review_sms_sent_at` at [route.ts:1081](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1081).
The enum part is muddier. Generated types include `visited_waiting_for_review` at [database.generated.ts:12588](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/types/database.generated.ts:12588), but the migration that adds it is [20260420000003_bookings_v05_foundations.sql:191](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20260420000003_bookings_v05_foundations.sql:191), dated April 20, 2026, which is after the April 11-12, 2026 incident. The squashed baseline still omits it at [20251123120000_squashed.sql:8576](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20251123120000_squashed.sql:8576). So enum-missing was plausible at incident time, but the repo cannot prove the live DB state.

4. Claim 4 — `CONFIRMED`.
`loadSentTableTemplateSet()` queries `messages.table_booking_id` and `template_key` directly at [route.ts:529](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:529). `recordOutboundSmsMessage()` mirrors `table_booking_id` and `template_key` into dedicated columns at [logging.ts:72](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/sms/logging.ts:72). If run 1 logged successfully, run 2 should skip at [route.ts:1130](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1130) before token creation. The code gives no path where a correctly logged prior send is ignored.
One nuance: review flows never `sentSet.add(...)` after a successful send, unlike reminders. That is a same-run weakness, not a next-run weakness.

5. Claim 5 — `DISPROVED`.
Option C is not the safest “simplest” fix. Body-sensitive dedupe is deliberate: the unit test at [safety.test.ts:110](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/sms/__tests__/safety.test.ts:110) explicitly asserts same key but different body => different `requestHash`. Stripping URLs globally changes semantics for every SMS template.
Concrete risk: `parking_payment_request` hashes a body containing a payment link at [payments.ts:181](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/parking/payments.ts:181) and [notifications.ts:7](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/parking/notifications.ts:7). A regenerated payment URL for the same booking would become a false duplicate. Same issue exists for waitlist offers with fresh secure URLs at [waitlist-offers.ts:244](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/events/waitlist-offers.ts:244).
The narrower fix for spam is not “check `review_sms_sent_at` more aggressively” because the code already does that twice. The narrower fix is to persist `review_sms_sent_at` independently and fail closed if booking-state or message logging persistence fails.

6. Missing analysis on message logging — `UNVERIFIABLE` historically, but the code implication is clear.
If messages were logged, `sentSet` should catch them. So the historical resends imply there was no usable `messages` row. In this path `customerId` is present at [route.ts:1165](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1165), so the code-based failure mode is `recordOutboundSmsMessage()` returning `null` after insert failure, surfaced as `logging_failed` at [twilio.ts:518](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/twilio.ts:518) and [twilio.ts:533](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/twilio.ts:533).
But I cannot prove the exact historical failure from repo alone. There is also a schema timeline inconsistency: the migration adding `messages.table_booking_id` and `template_key` is dated April 20, 2026 at [20260420000003_bookings_v05_foundations.sql:480](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20260420000003_bookings_v05_foundations.sql:480), while the older squashed baseline messages table lacks those columns at [20251123120000_squashed.sql:2018](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20251123120000_squashed.sql:2018). If those columns were truly missing live, though, `loadSentTableTemplateSet()` would also fail before sending, so the repo snapshot alone cannot reconstruct the live schema on April 11-12, 2026.

7. Actual cascade to 17 sends — `DISPROVED` as written; exact real-world chain is `UNVERIFIABLE`.
The spec’s loop is not code-consistent. After one successful send claims idempotency, the next attempt should hit `conflict` at [safety.ts:288](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/sms/safety.ts:288), return `idempotency_conflict` at [twilio.ts:304](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/lib/twilio.ts:304), and abort the cron at [route.ts:1202](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1202). With the repo defaults, I cannot derive repeated 15-minute sends from that.
A code-consistent repeated-send loop needs all three of these on every successful send:
1. `table_bookings` stays `confirmed` with `review_sms_sent_at=null` because [route.ts:1211](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1211) fails or affects zero rows.
2. No usable `messages` row exists, so [route.ts:529](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:529) and [route.ts:1130](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/api/cron/event-guest-engagement/route.ts:1130) do not suppress the rerun.
3. The idempotency claim is unavailable or gone between runs. The repo does not prove that. Default TTL is 14 days, and there is no in-repo cleanup caller.

Bottom line: the repo supports “state update failed” as a real duplicate-enabler, supports “unique URL causes idempotency conflict” as a real secondary effect, and does not support “TTL expiry between 15-minute runs” as the explanation. I did not inspect live Supabase data or deployed env vars, so the actual April 11-12, 2026 runtime state remains unverified.