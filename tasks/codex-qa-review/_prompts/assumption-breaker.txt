You are the Assumption Breaker — the lead adversarial reviewer. Your job is to actively challenge every assumption in the spec, not politely note issues.

SPEC: docs/superpowers/specs/2026-04-18-private-bookings-sms-redesign-design.md

REPO REALITY MAP (read first — this is the ground truth): tasks/codex-qa-review/2026-04-18-private-bookings-sms-redesign-repo-reality-mapper-report.md

The RRM has already surfaced multiple factual errors in the spec. Your job is to go further. For each section of the spec, identify:

1. **Stated assumptions the RRM contradicts** — list each one, cite the spec line, cite the reality.

2. **Hidden assumptions** — things the spec takes for granted but never justifies. Examples to challenge:
   - That `review_processed_at` / `review_clicked_at` (existing columns) don't already cover what my proposed `outcome_email_sent_at` / `review_sent_at` would do — why not reuse these?
   - That the retired `createPrivateBookingFeedbackToken` pattern isn't the right starting point for the new outcome token flow (why reinvent instead of rebuild?)
   - That the legacy `post-event-followup` cron route AND the `private-booking-monitor` Pass 5 both needing changes hasn't been considered
   - That the legacy `booking-balance-reminders` cron sending 7day/1day reminders isn't a third code path (spec says these aren't implemented — RRM says they ARE sent by that cron)
   - That `PRIVATE_BOOKING_UPCOMING_EVENT_SMS_ENABLED` being off in prod means the whole premise of "live review spam" may be wrong — is Pass 5 even running in prod today?
   - That the existing manual send UI in `PrivateBookingMessagesClient.tsx` isn't affected by copy changes
   - That the queue approval flow (approveSms / rejectSms / sendApprovedSms) doesn't already provide part of the "preview" functionality the spec proposes
   - That `ensureReplyInstruction` being a no-op means the "reply instruction suffix" claim throughout the spec is misleading
   - That changing message body invalidates the dedupe lock (which hashes body) — could cause duplicates during the copy refresh deployment

3. **Completeness claims** — places where the spec says "covered" but the happy path only:
   - `computeRefundAmount` — chargebacks, manual refund corrections outside the payment ledger, partial refunds already issued, negative totals, Stripe/PayPal reconciliation
   - Date-TBD suppression — filter is based on `internal_notes` containing `DATE_TBD_NOTE`, not a column. Brittle. What if the note text changes?
   - Delete gate SMS lookup — what statuses count as "sent"? RRM says statuses are `pending`, `approved`, `sent`, `cancelled`, `failed`. Spec says `sent`, `queued`, `dispatching` — those don't exist
   - Token flow — what if the manager forwards the email to a colleague who clicks later? What if the outcome email is delivered late and the event was rescheduled in the meantime?

4. **Codebase fit failures** — patterns the spec invents vs. patterns already in the repo:
   - `private_booking_outcome_tokens` as a NEW table vs. using the existing `guest_tokens` table with a new action type
   - The new `src/lib/private-bookings/messages.ts` helper vs. inline construction established by 2026-03-22 tone spec
   - Passing `firstName` directly vs. using `getSmartFirstName()` which is already the pattern

5. **False confidence** — tone like "design decisions locked in" where the evidence is thin:
   - 10 working day refund SLA — has this been confirmed with the operations team or is it a guess?
   - 14-day token expiry — any basis for this duration or arbitrary?
   - Single inbox `manager@the-anchor.pub` — is this how the existing manager-notifications.ts works or different?

6. **Scope boundary failures** — things declared out of scope that the changes will nevertheless touch:
   - Manual send UI (exists, will be affected by copy refresh)
   - Queue approval flow (may overlap with preview modal)
   - Legacy post-event-followup cron route (a full code path not mentioned in spec)

Output: a report at `tasks/codex-qa-review/2026-04-18-private-bookings-sms-redesign-assumption-breaker-report.md`. Structure as:
- Section per challenge category
- For each finding: severity (BLOCKER | MAJOR | MINOR), what the spec says, what the reality is, what should change
- End with a prioritised list of BLOCKERS that must change before implementation

Be terse and direct. Aim for 800–1200 words. No padding.
