You are the Repo Reality Mapper for an adversarial spec review. Your only output is a ground-truth map of the codebase areas the spec touches, so other reviewers can challenge spec claims against reality.

SPEC UNDER REVIEW: docs/superpowers/specs/2026-04-18-private-bookings-sms-redesign-design.md

Read that spec first, then verify every factual claim it makes about the codebase by reading the real files. Focus on:

1. **SMS pipeline**: src/lib/twilio.ts (quiet-hours handling via evaluateSmsQuietHours), src/services/sms-queue.ts (SmsQueueService, PRIVATE_BOOKING_SMS_AUTO_SEND_TRIGGERS, dedupe lock), src/lib/sms/support.ts (ensureReplyInstruction).

2. **Private bookings core**: src/services/private-bookings/mutations.ts (cancelBooking, deletePrivateBooking, status-change SMS triggers, booking_completed idempotency guard), src/services/private-bookings/payments.ts (deposit_received, final_payment_received, balance payment path), src/services/private-bookings.ts if separate, src/services/private-bookings/types.ts.

3. **Cron**: src/app/api/cron/private-booking-monitor/route.ts — list every PASS, every trigger_type it sends, every gating condition, every env flag (especially PRIVATE_BOOKING_UPCOMING_EVENT_SMS_ENABLED, MAX_PRIVATE_BOOKING_SMS_PER_RUN, PRIVATE_BOOKING_SEND_GUARD_*). Note whether Pass 5 currently sends review SMS ungated.

4. **DB schema**: inspect supabase/migrations/ for the private_bookings table — list every column. Verify presence or absence of: deposit_paid_date, deposit_amount, total_paid, balance_due_date, hold_expiry, is_date_tbd (or similar TBD flag), customer_first_name, contact_phone, event_date, status enum values. Also check for existing payment/ledger tables (private_booking_payments, private_booking_sms_queue structure).

5. **Tokens and throttling**: src/lib/guest/tokens.ts (createGuestToken, hashGuestToken), src/lib/guest/token-throttle.ts (checkGuestTokenThrottle scopes).

6. **Email**: src/lib/email/emailService.ts (sendEmail signature, Graph transport). Search for any existing use of manager@the-anchor.pub or similar manager inboxes — is this a pattern already in use?

7. **Related specs** in docs/superpowers/specs/: 2026-03-22-event-sms-cross-promotion-and-tone-refresh.md, 2026-03-19-private-bookings-payment-history.md, 2026-04-12-sms-pipeline-fixes-design.md — pull relevant constraints or prior decisions.

8. **Tests**: check existing Vitest coverage for any of the above (src/services/private-bookings/**/*.test.ts, src/lib/twilio*.test.ts, etc.).

9. **Any OTHER private-booking SMS paths I may have missed**: grep for sendSMS, queueAndSend, SmsQueueService in the full codebase. Flag anything the spec omits.

10. **feedback template**: src/lib/private-bookings/feedback.ts (PRIVATE_BOOKING_FEEDBACK_TEMPLATE_KEY) — what does this export?

OUTPUT FORMAT: a single markdown document with sections for each numbered area above. Under each section, list what exists (with file:line refs), what the spec claimed vs. what is actually there, and any constraints a downstream reviewer must know.

DO NOT critique. Just map. Let the Assumption Breaker do the critique.

Aim for 1000–1500 words. Use code fences for small code excerpts when helpful.
