1. [20260520000000_add_inclusion_type.sql:85](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20260520000000_add_inclusion_type.sql#L85), [20260520000000_add_inclusion_type.sql:214](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20260520000000_add_inclusion_type.sql#L214), [20260520000000_add_inclusion_type.sql:279](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/supabase/migrations/20260520000000_add_inclusion_type.sql#L279)

`portion_cost` is built from ingredient and recipe line costs. Ingredients use `quantity * unit_cost * 100 / yield_pct * (1 + wastage/100)`, where `unit_cost` is `cost_override` or `menu_get_latest_unit_cost(mi.id)`; recipes use the same formula but `unit_cost` is `cost_override` or `mr.portion_cost`. Aggregation is: sum all `included` and `removable`, add `MAX(line_cost)` per `choice` `option_group`, and exclude `upgrade` entirely. Actual nuance: `ingredient_wastage_pct` is selected, but because `dish_wastage_pct` is already `COALESCE(..., 0)`, the formula effectively uses the dish row wastage only.

`removable_allergens` is computed from non-`upgrade` ingredients/recipes only. For each allergen, it uses `BOOL_AND(...)`: every component carrying that allergen must either be `removable`, or be a `choice` item whose `option_group` has some other `choice` ingredient/recipe without that allergen. `removable_allergens` stores only the allergens where that boolean is `TRUE`. `is_modifiable_for` is `jsonb_object_agg(allergen || '_free', is_removable)`, so it includes both true and false keys like `{"gluten_free": true, "milk_free": false}`. Yes, the function always resets `allergen_verified = FALSE` and `allergen_verified_at = NULL`.

2. [DishCompositionTab.tsx:19](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishCompositionTab.tsx#L19), [DishCompositionTab.tsx:41](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishCompositionTab.tsx#L41)

`CostBreakdown` has these fields: `includedTotal`, `removableTotal`, `choiceGroups: Map<string, { maxCost; minCost; items: { name; cost }[] }>`, `upgradeGroups: Map<string, { maxCost; maxPrice; items: { name; cost; price }[] }>`, `ungroupedUpgrades: { name; cost; price }[]`, `baseTotal`, `upgradeTotal`.

`computeIngredientCost` routes `inclusion_type` like this: `included` adds to `includedTotal`; `removable` adds to `removableTotal`; `choice` groups by trimmed `option_group` (or `''`) and tracks `maxCost`/`minCost`; `upgrade` parses `upgrade_price` and either adds to grouped `upgradeGroups` or to `ungroupedUpgrades` if no group. Then `baseTotal = includedTotal + removableTotal + sum(maxCost of each choice group)`. `upgradeTotal` is not just upgrade cost; it is `baseTotal + sum(maxCost of each upgrade group) + sum(cost of ungrouped upgrades)`. Blank `inclusion_type` defaults to `included`; anything else falls through and is ignored.

3. [CompositionRow.tsx:30](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/CompositionRow.tsx#L30), [CompositionRow.tsx:189](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/CompositionRow.tsx#L189), [CompositionRow.tsx:289](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/CompositionRow.tsx#L289)

`IngredientCompositionRow` has a `Type` dropdown with `Included`, `Removable`, `Choice`, `Upgrade`. It shows a plain text `Group` input only for `choice`/`upgrade`, with a `datalist` from `existingGroups`. It shows `£ extra` only for `upgrade`.

When `inclusion_type` changes: switching to `included` or `removable` clears both `option_group` and `upgrade_price`; switching to `choice` clears only `upgrade_price`; switching to `upgrade` sets `upgrade_price` to `'0'` if it was blank and leaves `option_group` alone. Styling is type-based: `included` has no special badge/border; `removable` gets a dashed gray left border plus a gray `(removable)` badge; `choice` gets a colored left border and matching group badge based on a hash of the group name; `upgrade` gets an amber left border and an amber `Upgrade +£X.XX` badge.

4. [DishGpAnalysisTab.tsx:77](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishGpAnalysisTab.tsx#L77), [DishGpAnalysisTab.tsx:173](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishGpAnalysisTab.tsx#L173), [DishGpAnalysisTab.tsx:264](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishGpAnalysisTab.tsx#L264), [DishGpAnalysisTab.tsx:403](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishGpAnalysisTab.tsx#L403)

It has 3 sections: `Combinations`, `Upgrade Impact`, and `Allergen Summary`.

The allergen summary uses current form rows plus saved `dish.ingredients` / `dish.recipes`; it ignores `upgrade` rows. It builds `allergen -> components[]`, then separately builds `choiceGroupItems`. For each allergen, it marks it removable only if every component is either `removable`, or `choice` and has some other item in the same group without that allergen. The group lookup for a choice component is done by matching the component name back through the form rows, not by carrying the group directly on the allergen entry.

The upgrade section uses `baseCost = ingResult.baseTotal + recResult.baseTotal`, so base cost already includes worst-case `choice` costs. Each upgrade row computes GP on `sellingPrice + extraCharge` versus `baseCost + item.cost`. The “With all upgrades” footer takes `maxPrice` and `maxCost` per upgrade group, merges ingredient and recipe groups by group name, then adds all ungrouped upgrades.

5. [menu.ts:1048](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/services/menu.ts#L1048), [DishDrawer.tsx:174](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishDrawer.tsx#L174), [DishDrawer.tsx:303](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishDrawer.tsx#L303), [DishDrawer.tsx:554](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/(authenticated)/menu-management/dishes/_components/DishDrawer.tsx#L554)

API-side, `MenuService.getDishDetail` explicitly selects `option_group`, `inclusion_type`, and `upgrade_price` from both `menu_dish_ingredients` and `menu_dish_recipes`. In the drawer, `loadDishDetail()` hydrates rows as strings: `inclusion_type: row.inclusion_type || 'included'` and `upgrade_price: row.upgrade_price != null ? String(row.upgrade_price) : ''`.

On save, the drawer sends `inclusion_type` for every ingredient/recipe row, but only sends `upgrade_price` when `row.inclusion_type === 'upgrade'` and the field is non-empty; it only sends `option_group` for `choice` or `upgrade`, trimmed, otherwise `undefined`. The allergen verification button is in the top header summary, at the right beside the `Active` and `Sunday lunch` checkboxes; in edit mode it shows either a green `Allergens Verified` pill or an amber `Verify Allergens` button.

6. [reclassify-dish-compositions.ts:123](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/scripts/database/reclassify-dish-compositions.ts#L123), [reclassify-dish-compositions.ts:417](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/scripts/database/reclassify-dish-compositions.ts#L417), [reclassify-dish-compositions.ts:441](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/scripts/database/reclassify-dish-compositions.ts#L441), [reclassify-dish-compositions.ts:486](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/scripts/database/reclassify-dish-compositions.ts#L486)

Matching is two-stage: it finds the dish by exact lowercase/trimmed name through the `DISH_ALIASES` table, then for each rule does `rows.find(...)` on ingredient name regexes. That means it updates only the first matching ingredient row per rule, not all matches.

It is idempotent for updates: before pushing a change, it checks whether `inclusion_type`, `option_group`, and `upgrade_price` already equal the target state, and skips if so. Deletions are also effectively repeat-safe because a deleted row simply won’t be found next run. Missing dishes are handled gracefully: it collects them in `notFoundDishes`, prints a warning, and continues. Missing ingredient matches for individual rules are also silently skipped.

7. [menu-management.ts:495](/Users/peterpitcher/Cursor/OJ-AnchorManagementTools/src/app/actions/menu-management.ts#L495)

`verifyDishAllergens` does four things: checks permission, gets the current user, uses the admin Supabase client to update `menu_dishes` with `allergen_verified = true` and `allergen_verified_at = now`, then logs an audit event with `verified_by: user?.email` and revalidates `/menu-management/dishes` and `/menu-management`.

Yes, it has RBAC: it requires `checkUserPermission('menu_management', 'manage')`. If that fails, it returns `You do not have permission to manage dishes`.