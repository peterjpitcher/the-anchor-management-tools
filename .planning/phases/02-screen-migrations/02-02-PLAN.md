---
phase: 02-screen-migrations
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(authenticated)/parking/page.tsx
  - src/app/(authenticated)/parking/_components/ParkingClient.tsx
  - src/app/(authenticated)/menu-management/page.tsx
  - src/app/(authenticated)/menu-management/_components/MenuManagementClient.tsx
  - src/app/(authenticated)/table-bookings/page.tsx
  - src/app/(authenticated)/table-bookings/_components/TableBookingsClient.tsx
  - src/app/(authenticated)/table-bookings/_components/TimelineView.tsx
  - src/app/(authenticated)/table-bookings/_components/FloorPlanView.tsx
  - src/app/(authenticated)/table-bookings/_components/ListView.tsx
  - src/app/(authenticated)/table-bookings/_components/TablesFOH.tsx
  - src/app/(authenticated)/table-bookings/_components/TablesBOH.tsx
  - src/app/(authenticated)/table-bookings/_components/TablesReports.tsx
  - src/app/(authenticated)/table-bookings/_components/TablesSettings.tsx
  - src/app/(authenticated)/rota/page.tsx
  - src/app/(authenticated)/rota/_components/RotaClient.tsx
  - src/app/(authenticated)/rota/_components/RotaSchedule.tsx
  - src/app/(authenticated)/rota/_components/RotaLeave.tsx
  - src/app/(authenticated)/rota/_components/RotaTimeclock.tsx
  - src/app/(authenticated)/rota/_components/RotaLabourCosts.tsx
  - src/app/(authenticated)/rota/_components/RotaPayroll.tsx
  - src/app/(authenticated)/rota/_components/RotaTemplates.tsx
autonomous: true
requirements:
  - MIG-05
  - MIG-06
  - MIG-07
  - MIG-08

must_haves:
  truths:
    - "User navigates to Parking and sees table + lot map sidebar + pricing card layout"
    - "User navigates to Menu Management and sees section sidebar + dish table with availability toggles"
    - "User navigates to Table Bookings and can switch between Schedule (timeline/floor/list), FOH, BOH, Reports, Settings sub-pages"
    - "User navigates to Rota and can switch between Schedule, Leave, Timeclock, Labour Costs, Payroll, Templates sub-pages"
    - "All existing CRUD, filter, pagination, and export functionality works identically"
  artifacts:
    - path: "src/app/(authenticated)/parking/_components/ParkingClient.tsx"
      provides: "Parking page with lot map grid and pricing sidebar"
      exports: ["ParkingClient"]
    - path: "src/app/(authenticated)/table-bookings/_components/TableBookingsClient.tsx"
      provides: "Table Bookings with SectionNav routing to 5 sub-pages"
      exports: ["TableBookingsClient"]
    - path: "src/app/(authenticated)/table-bookings/_components/TimelineView.tsx"
      provides: "Timeline swimlane grid for table bookings schedule"
      exports: ["TimelineView"]
    - path: "src/app/(authenticated)/rota/_components/RotaClient.tsx"
      provides: "Rota with SectionNav routing to 6 sub-pages"
      exports: ["RotaClient"]
  key_links:
    - from: "src/app/(authenticated)/table-bookings/_components/TableBookingsClient.tsx"
      to: "TimelineView, FloorPlanView, ListView, TablesFOH, TablesBOH, TablesReports, TablesSettings"
      via: "SectionNav state switching"
      pattern: "useState.*activeSection"
    - from: "src/app/(authenticated)/rota/_components/RotaClient.tsx"
      to: "RotaSchedule, RotaLeave, RotaTimeclock, RotaLabourCosts, RotaPayroll, RotaTemplates"
      via: "SectionNav state switching"
      pattern: "useState.*activeSection"
---

<objective>
Migrate the 4 operations screens: Parking, Menu Management, Table Bookings (VERY HIGH complexity, 5 sub-pages), and Rota (HIGH complexity, 6 sub-pages). These are the most complex screens in the system.

Purpose: Operations screens are used daily by all staff. Table Bookings and Rota are the most complex screens with multiple sub-pages each. Migrating them validates the design system handles complex layouts.
Output: 4 fully migrated operations screens with all sub-pages using ds/ components exclusively.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-screen-migrations/02-CONTEXT.md
@.planning/phases/02-screen-migrations/02-RESEARCH.md
@.planning/phases/02-screen-migrations/02-01-SUMMARY.md

<interfaces>
<!-- ds/ components available after Plan 01 -->
Primitives: Button, Badge, Avatar, AvatarStack, Alert, Modal, Skeleton, Empty, Toast, Stat, Input, Select, Textarea, Checkbox, Radio, Switch, Field, ProgressBar, Spinner, SearchInput, Dropdown, DropdownItem, Tooltip, ConfirmDialog, FileUpload, Drawer, Stepper, DateTimePicker, Popover, IconButton
Composites: Card, CardHeader, CardBody, CardFooter, PageHeader, SectionNav, Tabs, Segmented, Table (+ Header/Body/Row/Head/Cell/Pagination), RevenueChart, Sparkline
Icons: Icon component with 47+ named icons
Tokens: colors, spacing, shadows, radii, easing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Parking and Menu Management screens (MIG-05, MIG-06)</name>
  <files>
    src/app/(authenticated)/parking/page.tsx,
    src/app/(authenticated)/parking/_components/ParkingClient.tsx,
    src/app/(authenticated)/menu-management/page.tsx,
    src/app/(authenticated)/menu-management/_components/MenuManagementClient.tsx
  </files>
  <read_first>
    src/app/(authenticated)/parking/page.tsx,
    src/app/(authenticated)/menu-management/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/parking.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/menu.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite both screens entirely. Per D-05: ALL sub-pages migrated.

    **Parking (MIG-05):**
    - PageHeader: breadcrumbs [{label:'Parking'}], title "Parking", subtitle with active count, actions: [Add Vehicle button, Export button]
    - Layout: `grid grid-cols-[1fr_320px] gap-6`
    - Left column: Card with search/filter bar (SearchInput + Select for status + date filter), table with registration, driver name, arrival, departure, status Badge, space number, actions Dropdown
    - Right column: Two stacked Cards:
      1. Lot Map Card: 6-column x 4-row grid of parking space cells. Each cell shows space number, colored by status (occupied=danger-soft, available=surface, reserved=warning-soft). Clickable to assign/release.
      2. Pricing Card: Rate tiers displayed as rows (duration + price), active tier highlighted

    **Menu Management (MIG-06):**
    - PageHeader: breadcrumbs [{label:'Menu'}], title "Menu Management", actions: [Add Dish button]
    - Layout: `grid grid-cols-[240px_1fr] gap-6`
    - Left sidebar: Card with section list. Each section is a clickable row with name + dish count Badge. Active section highlighted with primary-soft bg.
    - Right content: Segmented control (Table view / Card view) at top. Below:
      - Table view: dish table with name, description, price, allergen Badges, availability Switch toggle, actions Dropdown
      - Card view: grid of dish cards with image placeholder, name, price, allergens, availability Switch
    - All allergen tags rendered as Badge components with appropriate tones
    - Switch toggle calls existing server action to update dish availability (per D-09: no changes to server actions)

    **All imports from `@/ds` only.** Port data fetching from existing page.tsx files.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in src/app/\(authenticated\)/parking/_components/ParkingClient.tsx src/app/\(authenticated\)/menu-management/_components/MenuManagementClient.tsx; do echo "=== $f ==="; grep -c "from '@/ds'" "$f"; grep -c "ui-v2\|@/components/ui" "$f"; done && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ParkingClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - ParkingClient.tsx contains `grid grid-cols-[1fr_320px]` or equivalent 2-column layout
    - MenuManagementClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - MenuManagementClient.tsx contains `Segmented` usage (table/card view toggle)
    - MenuManagementClient.tsx contains `Switch` usage (availability toggle)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Parking and Menu Management fully rewritten with ds/ imports only. Parking shows lot map grid + pricing sidebar. Menu Management shows section sidebar + dish table with Segmented view toggle and Switch availability. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Table Bookings screen with all 5 sub-pages (MIG-07, pixel-perfect per D-04)</name>
  <files>
    src/app/(authenticated)/table-bookings/page.tsx,
    src/app/(authenticated)/table-bookings/_components/TableBookingsClient.tsx,
    src/app/(authenticated)/table-bookings/_components/TimelineView.tsx,
    src/app/(authenticated)/table-bookings/_components/FloorPlanView.tsx,
    src/app/(authenticated)/table-bookings/_components/ListView.tsx,
    src/app/(authenticated)/table-bookings/_components/TablesFOH.tsx,
    src/app/(authenticated)/table-bookings/_components/TablesBOH.tsx,
    src/app/(authenticated)/table-bookings/_components/TablesReports.tsx,
    src/app/(authenticated)/table-bookings/_components/TablesSettings.tsx
  </files>
  <read_first>
    src/app/(authenticated)/table-bookings/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/tables.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. **Per D-04:** Pixel-perfect fidelity. **Per D-05:** ALL 5 sub-pages migrated.

    This is the MOST COMPLEX screen. The design handoff (tables.jsx) shows:

    **TableBookingsClient.tsx** — Main container:
    - PageHeader: title "Tables", subtitle with date + cover count, actions: [New Booking, Walk-in, Waitlist buttons]
    - SectionNav: 5 items: Schedule, FOH, BOH, Reports, Settings
    - useState for activeSection, renders the matching sub-component

    **Schedule sub-page** (default) — Has its own Segmented control for 3 views:
    - Date navigation: prev/next arrows + date display + "Today" button
    - Segmented: Timeline / Floor Plan / List

    **TimelineView.tsx:**
    - CSS Grid: `grid-template-columns: 120px repeat(N, 1fr)` where N = number of 30-min time slots (e.g., 11am-11pm = 24 slots)
    - Row headers: table names (Table 1, Table 2, etc.) in the 120px column
    - Booking chips: positioned via `gridColumn: span 2` (1-hour bookings span 2 30-min slots). Each chip shows guest name, party size, colored by status.
    - Time slot mapping: map booking time to grid column index. The handoff uses a `slotMap` pattern.
    - Header row: time labels (11:00, 11:30, 12:00, etc.)

    **FloorPlanView.tsx:**
    - Absolute-positioned table elements on a fixed-size canvas (`relative w-full h-[500px] bg-surface-hover rounded-lg`)
    - Each table: `absolute` positioned circle/rectangle with table number, covers count, status color (available=green, occupied=orange, reserved=blue)
    - Table positions from data (x, y coordinates as percentages)
    - Read-only in Phase 2 (editable floor plan is VIEW-02, v2 scope)

    **ListView.tsx:**
    - Standard table view: time, table, guest name, party size, status Badge, phone, notes, actions Dropdown
    - SearchInput + Select filters at top

    **TablesFOH.tsx:**
    - FOH (Front of House) view: action-oriented cards for each active table
    - Grid of table cards showing: table number, current booking, time remaining, action chips (Seat, Clear, Transfer)
    - This is the content only — the chromeless FOH wrapper (MODE-01) is Phase 4

    **TablesBOH.tsx:**
    - BOH (Back of House) view: ticket-style cards for kitchen
    - Cards with course info, special requirements, allergen alerts, timing

    **TablesReports.tsx:**
    - Booking channel breakdown: ProgressBar bars for each channel (Online, Phone, Walk-in, etc.)
    - Cover stats, peak time analysis, no-show rate

    **TablesSettings.tsx:**
    - Form fields for service windows (DateTimePicker for start/end times)
    - Table configuration: table name, capacity, section assignment
    - Online booking settings: lead time, max party size, confirmation message

    **All imports from `@/ds` only.** Port all data fetching and server action calls from existing code.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && grep -c "from '@/ds'" src/app/\(authenticated\)/table-bookings/_components/TableBookingsClient.tsx && grep -c "ui-v2\|@/components/ui" src/app/\(authenticated\)/table-bookings/_components/*.tsx && ls src/app/\(authenticated\)/table-bookings/_components/ | grep -c "\.tsx" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TableBookingsClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - TableBookingsClient.tsx contains `SectionNav` usage with 5 items
    - TimelineView.tsx exists and contains `grid-template-columns` or `gridTemplateColumns`
    - FloorPlanView.tsx exists and contains `absolute` positioning
    - ListView.tsx exists and contains `Table` usage from ds/
    - TablesFOH.tsx, TablesBOH.tsx, TablesReports.tsx, TablesSettings.tsx all exist
    - All _components/*.tsx files have zero `ui-v2` imports (grep returns 0)
    - At least 8 .tsx files exist in table-bookings/_components/ directory
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Table Bookings fully rewritten with all 5 sub-pages (Schedule with timeline/floor/list views, FOH, BOH, Reports, Settings). Timeline swimlane grid renders booking chips correctly. All ds/ imports, zero ui-v2 imports. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Rota screen with all 6 sub-pages (MIG-08)</name>
  <files>
    src/app/(authenticated)/rota/page.tsx,
    src/app/(authenticated)/rota/_components/RotaClient.tsx,
    src/app/(authenticated)/rota/_components/RotaSchedule.tsx,
    src/app/(authenticated)/rota/_components/RotaLeave.tsx,
    src/app/(authenticated)/rota/_components/RotaTimeclock.tsx,
    src/app/(authenticated)/rota/_components/RotaLabourCosts.tsx,
    src/app/(authenticated)/rota/_components/RotaPayroll.tsx,
    src/app/(authenticated)/rota/_components/RotaTemplates.tsx
  </files>
  <read_first>
    src/app/(authenticated)/rota/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/rota.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. **Per D-05:** ALL 6 sub-pages migrated.

    Read the design handoff rota.jsx for exact layout. The Rota has:

    **RotaClient.tsx** — Main container:
    - PageHeader: title "Rota", subtitle with week date range, actions: [Publish, Auto-fill, Print buttons]
    - SectionNav: 6 items: Schedule, Leave, Timeclock, Labour Costs, Payroll, Templates
    - useState for activeSection, renders matching sub-component

    **RotaSchedule.tsx:**
    - Week navigation: prev/next arrows + week display + "This Week" button
    - Weekly grid: CSS Grid with `grid-template-columns: 200px repeat(7, 1fr)` — employee names in first column, 7 day columns
    - Each cell contains shift chips: colored blocks showing shift time (e.g., "9-5"), role badge
    - Employee rows: Avatar + name in 200px column
    - Totals row at bottom: daily hour totals
    - Click on cell to add/edit shift (opens Modal with shift form)

    **RotaLeave.tsx:**
    - Leave requests table: employee name (Avatar), type (Annual/Sick/Other Badge), start date, end date, days, status Badge (Pending/Approved/Rejected), actions Dropdown (Approve/Reject)
    - Tabs: Pending / Approved / All

    **RotaTimeclock.tsx:**
    - Timeclock records table: employee name (Avatar), date, clock in time, clock out time, total hours, status Badge (On Time/Late/Early), actions Dropdown
    - Date filter + employee SearchInput

    **RotaLabourCosts.tsx:**
    - Summary stats: 4-col Stat grid (Total Hours, Labour Cost, Avg Hourly, Cost vs Revenue %)
    - Breakdown table: employee name, scheduled hours, actual hours, variance, cost
    - Weekly cost ProgressBar comparison

    **RotaPayroll.tsx:**
    - Payroll runs table: period, status Badge (Draft/Pending/Paid), total amount, employee count, actions Dropdown (View/Export/Submit)
    - Current period summary Card

    **RotaTemplates.tsx:**
    - Template list: Card for each template with name, description, shift count
    - Click to load template into schedule
    - Actions: Edit, Duplicate, Delete (via Dropdown)

    **All imports from `@/ds` only.** Port all data fetching and server action calls from existing rota code.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && grep -c "from '@/ds'" src/app/\(authenticated\)/rota/_components/RotaClient.tsx && grep -c "ui-v2\|@/components/ui" src/app/\(authenticated\)/rota/_components/*.tsx && ls src/app/\(authenticated\)/rota/_components/ | grep -c "\.tsx" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RotaClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - RotaClient.tsx contains `SectionNav` usage with 6 items
    - RotaSchedule.tsx exists and contains `grid-template-columns` or `gridTemplateColumns` with `200px repeat(7`
    - RotaLeave.tsx, RotaTimeclock.tsx, RotaLabourCosts.tsx, RotaPayroll.tsx, RotaTemplates.tsx all exist
    - All _components/*.tsx files have zero `ui-v2` imports (grep returns 0)
    - At least 7 .tsx files exist in rota/_components/ directory
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Rota fully rewritten with all 6 sub-pages (Schedule with weekly grid, Leave, Timeclock, Labour Costs, Payroll, Templates). Weekly grid renders employee x day shift chips correctly. All ds/ imports, zero ui-v2 imports. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -r "ui-v2\|@/components/ui" src/app/(authenticated)/parking/ src/app/(authenticated)/menu-management/ src/app/(authenticated)/table-bookings/ src/app/(authenticated)/rota/ --include="*.tsx"` returns zero results
- All 4 operations screens render with correct layouts and sub-page navigation
</verification>

<success_criteria>
- Parking migrated with lot map grid + pricing sidebar layout
- Menu Management migrated with section sidebar + dish table + Segmented view toggle + Switch availability
- Table Bookings migrated with all 5 sub-pages including timeline swimlane, floor plan, list view, FOH, BOH, Reports, Settings
- Rota migrated with all 6 sub-pages including weekly grid, leave, timeclock, labour costs, payroll, templates
- Zero ui-v2 imports in any migrated page
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-screen-migrations/02-02-SUMMARY.md`
</output>
