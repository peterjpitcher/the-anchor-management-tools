---
phase: 02-screen-migrations
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(authenticated)/invoices/page.tsx
  - src/app/(authenticated)/invoices/_components/InvoicesClient.tsx
  - src/app/(authenticated)/quotes/page.tsx
  - src/app/(authenticated)/quotes/_components/QuotesClient.tsx
  - src/app/(authenticated)/receipts/page.tsx
  - src/app/(authenticated)/receipts/_components/ReceiptsClient.tsx
  - src/app/(authenticated)/mileage/page.tsx
  - src/app/(authenticated)/mileage/_components/MileageClient.tsx
  - src/app/(authenticated)/expenses/page.tsx
  - src/app/(authenticated)/expenses/_components/ExpensesClient.tsx
  - src/app/(authenticated)/mgd/page.tsx
  - src/app/(authenticated)/mgd/_components/MgdClient.tsx
autonomous: true
requirements:
  - MIG-09
  - MIG-10
  - MIG-11
  - MIG-12
  - MIG-13
  - MIG-14

must_haves:
  truths:
    - "User navigates to Invoices and sees table with tabs (All/Drafts/Sent/Paid/Overdue) and shared SectionNav with Quotes"
    - "User navigates to Quotes and sees table with status tracking and shared SectionNav with Invoices"
    - "User navigates to Receipts and sees upload dropzone card + receipt table with tabs"
    - "User navigates to Mileage and can switch between Trips, Destinations, Rates sub-pages via SectionNav"
    - "User navigates to Expenses and sees table + category breakdown sidebar with budget ProgressBars"
    - "User navigates to MGD and sees machines table, quarterly readings, return history, HMRC registration"
    - "Invoices and Quotes share SectionNav with correct active state based on pathname"
  artifacts:
    - path: "src/app/(authenticated)/invoices/_components/InvoicesClient.tsx"
      provides: "Invoices page with shared SectionNav and tabbed table"
      exports: ["InvoicesClient"]
    - path: "src/app/(authenticated)/receipts/_components/ReceiptsClient.tsx"
      provides: "Receipts page with FileUpload dropzone and table"
      exports: ["ReceiptsClient"]
    - path: "src/app/(authenticated)/mileage/_components/MileageClient.tsx"
      provides: "Mileage page with 3-sub-page SectionNav"
      exports: ["MileageClient"]
    - path: "src/app/(authenticated)/expenses/_components/ExpensesClient.tsx"
      provides: "Expenses page with category sidebar ProgressBars"
      exports: ["ExpensesClient"]
  key_links:
    - from: "InvoicesClient.tsx"
      to: "QuotesClient.tsx"
      via: "Shared SectionNav with href links and pathname-based active state"
      pattern: "SectionNav.*items.*Invoices.*Quotes"
    - from: "ReceiptsClient.tsx"
      to: "src/ds/primitives/FileUpload.tsx"
      via: "FileUpload component for receipt upload"
      pattern: "FileUpload"
    - from: "ExpensesClient.tsx"
      to: "src/ds/primitives/ProgressBar.tsx"
      via: "Budget progress bars in category sidebar"
      pattern: "ProgressBar"
---

<objective>
Migrate all 6 finance screens: Invoices, Quotes, Receipts, Mileage, Expenses, and MGD. These screens share patterns (tables, filters, SectionNav) and have moderate complexity.

Purpose: Finance screens form a coherent group with shared navigation patterns (Invoices/Quotes SectionNav, Receipts tabs). Migrating as a batch ensures consistent treatment.
Output: 6 fully migrated finance screens with all sub-pages using ds/ components exclusively.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-screen-migrations/02-CONTEXT.md
@.planning/phases/02-screen-migrations/02-RESEARCH.md
@.planning/phases/02-screen-migrations/02-01-SUMMARY.md

<interfaces>
<!-- ds/ components available after Plan 01 -->
Primitives: Button, Badge, Avatar, AvatarStack, Alert, Modal, Skeleton, Empty, Toast, Stat, Input, Select, Textarea, Checkbox, Radio, Switch, Field, ProgressBar, Spinner, SearchInput, Dropdown, DropdownItem, Tooltip, ConfirmDialog, FileUpload, Drawer, Stepper, DateTimePicker, Popover, IconButton
Composites: Card, CardHeader, CardBody, CardFooter, PageHeader, SectionNav, Tabs, Segmented, Table (+ Header/Body/Row/Head/Cell/Pagination), RevenueChart, Sparkline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Invoices and Quotes with shared SectionNav (MIG-09, MIG-10)</name>
  <files>
    src/app/(authenticated)/invoices/page.tsx,
    src/app/(authenticated)/invoices/_components/InvoicesClient.tsx,
    src/app/(authenticated)/quotes/page.tsx,
    src/app/(authenticated)/quotes/_components/QuotesClient.tsx
  </files>
  <read_first>
    src/app/(authenticated)/invoices/page.tsx,
    src/app/(authenticated)/quotes/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/invoices.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/quotes.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. **Per D-05:** ALL sub-pages migrated.

    **CRITICAL PITFALL (from research Pitfall 2):** Invoices and Quotes share the same SectionNav with 6 items: Invoices, Quotes, Catalog, Recurring, Vendors, Export. The SectionNav must use `href` links (not client-side state) so active state carries correctly across page transitions. Determine active state from `usePathname()` matching against each item's href.

    **Invoices (MIG-09):**
    - PageHeader: breadcrumbs [{label:'Invoices'}], title "Invoices", subtitle with count + total amount, actions: [New Invoice button, Export button]
    - SectionNav: 6 items with href links: `/invoices` (Invoices), `/quotes` (Quotes), `/invoices/catalog` (Catalog), `/invoices/recurring` (Recurring), `/invoices/vendors` (Vendors, if exists), `/invoices/export` (Export). Active determined by pathname.
    - Tabs: All / Drafts / Sent / Paid / Overdue (filter the table)
    - Card with: search/filter bar (SearchInput + Select for client), table with invoice number, client (Avatar + name), amount, date, due date, status Badge (Draft/Sent/Paid/Overdue), actions Dropdown (View/Edit/Send/Download PDF/Delete)
    - TablePagination footer

    **Quotes (MIG-10):**
    - PageHeader: breadcrumbs [{label:'Quotes'}], title "Quotes", subtitle with count, actions: [New Quote button]
    - Same SectionNav as Invoices (6 items) — active on "Quotes" when at /quotes path
    - Table with: quote number, client name (Avatar), amount, date, status Badge (Draft/Sent/Accepted/Rejected/Expired), actions Dropdown (View/Edit/Send/Convert to Invoice/Delete)
    - TablePagination footer

    **Both pages share the SectionNav pattern.** Create a shared constant for the SectionNav items array and import it in both client files, OR duplicate the items array in each (simpler, no shared file needed since the items are static).

    **Per D-09:** No changes to server actions. Port existing data fetching.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in src/app/\(authenticated\)/invoices/_components/InvoicesClient.tsx src/app/\(authenticated\)/quotes/_components/QuotesClient.tsx; do echo "=== $f ==="; grep -c "from '@/ds'" "$f"; grep -c "ui-v2\|@/components/ui" "$f"; grep -c "SectionNav" "$f"; done && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - InvoicesClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - InvoicesClient.tsx contains `SectionNav` usage
    - InvoicesClient.tsx contains `Tabs` usage
    - QuotesClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - QuotesClient.tsx contains `SectionNav` usage
    - Both files reference shared SectionNav items including "Invoices" and "Quotes"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Invoices and Quotes fully rewritten with shared SectionNav (6 items, href-based active state). Invoices shows tabbed table, Quotes shows status-tracked table. All ds/ imports, zero ui-v2. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Receipts, Mileage, Expenses, MGD screens (MIG-11, MIG-12, MIG-13, MIG-14)</name>
  <files>
    src/app/(authenticated)/receipts/page.tsx,
    src/app/(authenticated)/receipts/_components/ReceiptsClient.tsx,
    src/app/(authenticated)/mileage/page.tsx,
    src/app/(authenticated)/mileage/_components/MileageClient.tsx,
    src/app/(authenticated)/expenses/page.tsx,
    src/app/(authenticated)/expenses/_components/ExpensesClient.tsx,
    src/app/(authenticated)/mgd/page.tsx,
    src/app/(authenticated)/mgd/_components/MgdClient.tsx
  </files>
  <read_first>
    src/app/(authenticated)/receipts/page.tsx,
    src/app/(authenticated)/mileage/page.tsx,
    src/app/(authenticated)/expenses/page.tsx,
    src/app/(authenticated)/mgd/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/receipts.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/mileage.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/expenses.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/mgd.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite all 4 screens entirely. **Per D-05:** ALL sub-pages migrated.
    **Per D-03:** Legacy ui/ imports in mileage/expenses/mgd are NOT cleaned up. Only replace ui-v2 imports with ds/. If these files import from `@/components/ui/` (legacy), leave those imports as-is.

    **Receipts (MIG-11):**
    - PageHeader: breadcrumbs [{label:'Receipts'}], title "Receipts", actions: [Export button]
    - Tabs: Receipts / Expenses / Mileage / Reconciliation (these may link to other pages or filter content)
    - Layout: `grid grid-cols-[260px_1fr] gap-6`
    - Left: FileUpload dropzone Card (drag-and-drop area for receipt upload, file type hint "JPEG, PNG, or PDF up to 10MB")
    - Right: Card with table of uploaded receipts: date, vendor, amount, category Badge, OCR description, status Badge (Pending/Approved/Rejected), actions Dropdown
    - Upload flow: drag file to dropzone -> calls existing upload server action -> table refreshes

    **Mileage (MIG-12):**
    - PageHeader: breadcrumbs [{label:'Mileage'}], title "Mileage", subtitle with total claims, actions: [New Trip button, Export button]
    - SectionNav: 3 items: Trips, Destinations, Rates
    - Trips sub-page: table with date, from/to, distance, rate, amount, status Badge, actions Dropdown. SearchInput + date filter.
    - Destinations sub-page: table of saved destinations with name, address, distance. Add/Edit/Delete via Dropdown.
    - Rates sub-page: HMRC rates form showing current rate tiers (Field + Input for threshold miles, standard rate, reduced rate). Display-only or editable depending on permissions.

    **Expenses (MIG-13):**
    - PageHeader: breadcrumbs [{label:'Expenses'}], title "Expenses", actions: [New Expense button, Export button]
    - Layout: `grid grid-cols-[1fr_320px] gap-6`
    - Left: Card with table of expenses: date, description, category Badge, amount, receipt attached indicator, status Badge, actions Dropdown
    - Right: Category breakdown Card with ProgressBar bars for each category (Food, Travel, Office, etc.) showing spend vs budget. Each row: category name, spend amount, budget amount, ProgressBar (percent = spend/budget * 100, tone=danger if >90%, warning if >75%, primary otherwise)

    **MGD (MIG-14):**
    - PageHeader: breadcrumbs [{label:'MGD'}], title "Machine Games Duty", actions: [New Machine button, Export button]
    - Alert (info tone) at top explaining MGD reporting requirements
    - Machines table: Card with table of machines (name, type, location, status Badge, last reading date)
    - Layout below machines: `grid grid-cols-[1.4fr_1fr] gap-6`
    - Left: Quarterly readings table (quarter, gross takings, net takings, duty amount)
    - Right: Return history Cards (each card = one submitted return with quarter, amount, status Badge, submission date)
    - HMRC Registration section: Card with registration form fields (Field + Input for registration number, effective date, etc.)

    **All imports from `@/ds` only (except legacy ui/ per D-03).** Port data fetching from existing files.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in src/app/\(authenticated\)/receipts/_components/ReceiptsClient.tsx src/app/\(authenticated\)/mileage/_components/MileageClient.tsx src/app/\(authenticated\)/expenses/_components/ExpensesClient.tsx src/app/\(authenticated\)/mgd/_components/MgdClient.tsx; do echo "=== $f ==="; grep -c "from '@/ds'" "$f"; grep -c "ui-v2" "$f"; done && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ReceiptsClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - ReceiptsClient.tsx contains `FileUpload` usage
    - MileageClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - MileageClient.tsx contains `SectionNav` usage with 3 items
    - ExpensesClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - ExpensesClient.tsx contains `ProgressBar` usage
    - ExpensesClient.tsx contains `grid grid-cols-[1fr_320px]` or equivalent 2-column layout
    - MgdClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - MgdClient.tsx contains `Alert` usage (info tone at top)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Receipts, Mileage, Expenses, and MGD fully rewritten with ds/ imports. Receipts has FileUpload dropzone. Mileage has 3-sub-page SectionNav. Expenses has category ProgressBar sidebar. MGD has machines table + readings + returns + registration. Zero ui-v2 imports. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -r "ui-v2" src/app/(authenticated)/invoices/ src/app/(authenticated)/quotes/ src/app/(authenticated)/receipts/ src/app/(authenticated)/mileage/ src/app/(authenticated)/expenses/ src/app/(authenticated)/mgd/ --include="*.tsx"` returns zero results
- Invoices and Quotes both render SectionNav with shared items
</verification>

<success_criteria>
- Invoices and Quotes migrated with shared SectionNav (href-based active state)
- Receipts migrated with FileUpload dropzone + table
- Mileage migrated with 3-sub-page SectionNav (Trips, Destinations, Rates)
- Expenses migrated with category breakdown sidebar using ProgressBars
- MGD migrated with machines table, readings, returns, and registration form
- Zero ui-v2 imports in any migrated page
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-screen-migrations/02-03-SUMMARY.md`
</output>
