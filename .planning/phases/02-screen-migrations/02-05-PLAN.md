---
phase: 02-screen-migrations
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/auth/login/page.tsx
  - src/app/auth/login/_components/LoginClient.tsx
  - src/app/(employee-onboarding)/onboarding/[token]/page.tsx
  - src/app/(employee-onboarding)/onboarding/[token]/_components/OnboardingClient.tsx
  - src/app/(staff-portal)/portal/page.tsx
  - src/app/(staff-portal)/portal/_components/PortalClient.tsx
  - src/app/(timeclock)/timeclock/page.tsx
  - src/app/(timeclock)/timeclock/_components/TimeclockClient.tsx
  - src/app/table-booking/page.tsx
  - src/app/table-booking/_components/PublicBookingClient.tsx
  - src/app/parking/guest/[id]/page.tsx
  - src/app/parking/guest/[id]/_components/PublicParkingClient.tsx
  - src/app/booking-confirmation/[token]/page.tsx
  - src/app/booking-confirmation/[token]/_components/BookingConfirmationClient.tsx
  - src/app/privacy/page.tsx
  - src/app/error/page.tsx
  - src/app/error/_components/ErrorClient.tsx
  - src/app/unauthorized/page.tsx
autonomous: true
requirements:
  - MIG-19
  - MIG-20
  - MIG-21
  - MIG-22
  - MIG-23
  - MIG-24
  - MIG-25
  - MIG-26
  - MIG-27
  - MIG-28

must_haves:
  truths:
    - "User visits Login and sees centered auth card with email/password fields, 2FA step, Microsoft SSO button using .auth layout CSS"
    - "User visits Onboarding and sees 6-step wizard with Stepper rail, file upload step, contract scroll-and-sign step using .onboard layout CSS"
    - "User visits Staff Portal and sees greeting, clock-in card, shifts, leave using .portal layout CSS"
    - "User visits Timeclock and sees full-screen dark kiosk with staff grid and large clock using .kiosk layout CSS"
    - "User visits Public Booking and sees hero banner, 3-step wizard, time slots using .public layout CSS"
    - "User visits Public Parking and sees hero banner, form fields, pricing breakdown using .public layout CSS"
    - "User visits Booking Confirmation and sees success state with QR ticket stub using .public layout CSS"
    - "User visits Privacy page and sees prose layout using .public--slim layout CSS"
    - "User visits Error page and sees centered error card with reference code using .auth layout CSS"
    - "User visits Unauthorised page and sees access denied card with attempted path using .auth layout CSS"
    - "None of these pages render inside the AppShell sidebar"
  artifacts:
    - path: "src/app/auth/login/_components/LoginClient.tsx"
      provides: "Login page with auth card, 2FA, SSO"
      exports: ["LoginClient"]
    - path: "src/app/(employee-onboarding)/onboarding/[token]/_components/OnboardingClient.tsx"
      provides: "6-step onboarding wizard"
      exports: ["OnboardingClient"]
    - path: "src/app/(staff-portal)/portal/_components/PortalClient.tsx"
      provides: "Staff portal with clock-in and shifts"
      exports: ["PortalClient"]
    - path: "src/app/(timeclock)/timeclock/_components/TimeclockClient.tsx"
      provides: "Timeclock kiosk with dark layout and staff grid"
      exports: ["TimeclockClient"]
  key_links:
    - from: "Login page"
      to: "globals.css .auth classes"
      via: "CSS class application"
      pattern: "className.*auth"
    - from: "Onboarding page"
      to: "globals.css .onboard classes"
      via: "CSS class application"
      pattern: "className.*onboard"
    - from: "Timeclock page"
      to: "globals.css .kiosk classes"
      via: "CSS class application"
      pattern: "className.*kiosk"
    - from: "Public Booking page"
      to: "globals.css .public classes"
      via: "CSS class application"
      pattern: "className.*public"
---

<objective>
Migrate all auth, public, and standalone-layout screens: Login, Onboarding, Staff Portal, Timeclock Kiosk, Public Booking, Public Parking, Booking Confirmation, Privacy, Error, and Unauthorised. These 10 screens use standalone layouts (NOT the AppShell sidebar).

Purpose: Completes the full 28-screen migration. These screens use the 5 standalone layout CSS classes added in Plan 01 (.auth, .public, .portal, .kiosk, .onboard). They do NOT render inside the AppShell.
Output: 10 fully migrated standalone-layout screens using ds/ components and layout CSS classes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-screen-migrations/02-CONTEXT.md
@.planning/phases/02-screen-migrations/02-RESEARCH.md
@.planning/phases/02-screen-migrations/02-01-SUMMARY.md

<interfaces>
<!-- ds/ components available after Plan 01 -->
Primitives: Button, Badge, Avatar, AvatarStack, Alert, Modal, Skeleton, Empty, Toast, Stat, Input, Select, Textarea, Checkbox, Radio, Switch, Field, ProgressBar, Spinner, SearchInput, Dropdown, DropdownItem, Tooltip, ConfirmDialog, FileUpload, Drawer, Stepper, DateTimePicker, Popover, IconButton
Composites: Card, CardHeader, CardBody, CardFooter, PageHeader, SectionNav, Tabs, Segmented, Table (+ Header/Body/Row/Head/Cell/Pagination), RevenueChart, Sparkline

<!-- Layout CSS classes available in globals.css after Plan 01 -->
.auth, .auth__card, .auth__brand, .auth__logo, .auth__title, .auth__sub, .auth__h1, .auth__lead, .auth__link, .auth__divider, .auth__footer
.public, .public__hero, .public__hero--slim, .public__hero-bg, .public__hero-inner, .public__brand-mini, .public__hero-title, .public__hero-sub, .public__main, .public__main--prose, .public__card, .public__h2, .public__steps, .public__step, .public__chip, .public__date, .public__slot, .public__ticket, .public__summary, .public__assure, .public__footer, .public__prose
.portal, .portal__topbar, .portal__brand, .portal__body, .portal__greeting, .portal__grid
.kiosk, .kiosk__header, .kiosk__brand, .kiosk__clock, .kiosk__time, .kiosk__date, .kiosk__stats, .kiosk__title, .kiosk__grid, .kiosk__card, .kiosk__name, .kiosk__role, .kiosk__state, .kiosk__dot, .kiosk__footer
.onboard, .onboard__topbar, .onboard__brand, .onboard__body, .onboard__rail, .onboard__step, .onboard__step-bullet, .onboard__step-label, .onboard__main, .onboard__h1, .onboard__nav
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Login, Error, Unauthorised screens using .auth layout (MIG-19, MIG-27, MIG-28)</name>
  <files>
    src/app/auth/login/page.tsx,
    src/app/auth/login/_components/LoginClient.tsx,
    src/app/error/page.tsx,
    src/app/error/_components/ErrorClient.tsx,
    src/app/unauthorized/page.tsx
  </files>
  <read_first>
    src/app/auth/login/page.tsx,
    src/app/error/page.tsx,
    src/app/globals.css,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/login.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/error.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/unauthorized.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. All 3 screens use the `.auth` standalone layout (centered card, NO sidebar).
    **CRITICAL PITFALL (from research Pitfall 6):** These pages must NOT render inside AppShell. They have their own route groups outside `(authenticated)/`.

    **Login (MIG-19):**
    - Outer container: `<div className="auth">`
    - Brand header: `<div className="auth__brand">` with logo + "The Anchor" title
    - Auth card: `<div className="auth__card">` centered on page
    - Step 1 (email/password): `<h1 className="auth__h1">Sign in</h1>`, `<p className="auth__lead">Enter your credentials</p>`, Field+Input for email, Field+Input for password (type=password), "Forgot password?" link (`auth__link`), Sign In Button (primary, full width)
    - Divider: `<div className="auth__divider">or</div>`
    - Microsoft SSO: Button (secondary, full width, icon=microsoft) "Sign in with Microsoft 365"
    - Step 2 (2FA — shown conditionally when server responds with 2FA required): `<h1 className="auth__h1">Two-factor authentication</h1>`, 6-digit code input (6 individual Input fields or single Input), Verify Button
    - Footer: `<div className="auth__footer">` with links

    Port existing auth logic (Supabase Auth calls, redirect on success, error handling). Do NOT change the actual auth flow — UI-only migration.

    **Error (MIG-27):**
    - Outer: `<div className="auth">`
    - Card: `<div className="auth__card">` with Icon (alert-circle, danger tone, large), `<h1>Something went wrong</h1>`, error message paragraph, reference code box (`<div className="bg-surface-hover rounded-lg p-3 font-mono text-sm">REF-{code}</div>`), expandable technical details (optional), retry Button (primary) + "Back to Dashboard" Button (secondary)

    **Unauthorised (MIG-28):**
    - Outer: `<div className="auth">`
    - Card: `<div className="auth__card">` with Icon (shield, warning tone, large), `<h1>Access Denied</h1>`, message paragraph, attempted path box (`<div className="bg-surface-hover rounded-lg p-3 font-mono text-sm">{attemptedPath}</div>`), permission info text, "Go Back" Button (secondary) + "Dashboard" Button (primary)
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in src/app/auth/login/_components/LoginClient.tsx src/app/error/_components/ErrorClient.tsx src/app/unauthorized/page.tsx; do echo "=== $f ==="; test -f "$f" && echo "EXISTS" || echo "MISSING"; done && grep -c "auth__card\|auth__brand\|className.*auth" src/app/auth/login/_components/LoginClient.tsx 2>/dev/null && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - LoginClient.tsx exists with `className` containing `auth` layout classes (auth__card, auth__h1, etc.)
    - LoginClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - LoginClient.tsx contains `Field` and `Input` usage for email/password
    - ErrorClient.tsx or error/page.tsx contains `auth` layout classes
    - unauthorized/page.tsx contains `auth` layout classes and attempted path display
    - None of these files import from AppShell
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Login with auth card + 2FA + SSO, Error with reference code card, Unauthorised with attempted path card -- all using .auth layout CSS. No AppShell imports. All ds/ imports, zero ui-v2. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Onboarding, Staff Portal, Timeclock Kiosk screens (MIG-20, MIG-21, MIG-22)</name>
  <files>
    src/app/(employee-onboarding)/onboarding/[token]/page.tsx,
    src/app/(employee-onboarding)/onboarding/[token]/_components/OnboardingClient.tsx,
    src/app/(staff-portal)/portal/page.tsx,
    src/app/(staff-portal)/portal/_components/PortalClient.tsx,
    src/app/(timeclock)/timeclock/page.tsx,
    src/app/(timeclock)/timeclock/_components/TimeclockClient.tsx
  </files>
  <read_first>
    src/app/(employee-onboarding)/onboarding/[token]/page.tsx,
    src/app/(staff-portal)/portal/page.tsx,
    src/app/(timeclock)/timeclock/page.tsx,
    src/app/globals.css,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/onboarding.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/staff-portal.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/timeclock.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. **Per D-05:** ALL sub-pages migrated.
    Each screen uses its own standalone layout CSS.

    **Onboarding (MIG-20):**
    - Outer: `<div className="onboard">`
    - Top bar: `<div className="onboard__topbar">` with brand logo
    - Body: `<div className="onboard__body">` with 2-column layout:
      - Rail sidebar: `<nav className="onboard__rail">` with Stepper component showing 6 steps: Welcome, Personal, Right to Work, Bank Details, Contract, Done
      - Main content: `<div className="onboard__main">` with current step content
    - **Step 1 (Welcome):** Heading, intro text, "Get Started" Button
    - **Step 2 (Personal):** Field + Input for first name, last name, email, phone, date of birth, address. "Next" Button
    - **Step 3 (Right to Work):** FileUpload dropzone for passport/visa/RTW document. File type hint. "Next" Button
    - **Step 4 (Bank Details):** Field + Input for sort code, account number, account holder name. "Next" Button
    - **Step 5 (Contract):** Scrollable contract text in a `<div className="max-h-[400px] overflow-y-auto border border-border rounded-lg p-4">`, Checkbox "I have read and agree to the terms", "Sign & Continue" Button (disabled until checkbox checked)
    - **Step 6 (Done):** Success icon, "You're all set!" heading, summary of what happens next, "Go to Staff Portal" Button
    - Navigation: `<div className="onboard__nav">` with Back + Next buttons, step indicator

    **Staff Portal (MIG-21):**
    - Outer: `<div className="portal">`
    - Top bar: `<div className="portal__topbar">` with brand "The Anchor - Staff" + sign out link
    - Body: `<div className="portal__body">`
    - Greeting: `<div className="portal__greeting">` "Good morning, {name}"
    - Clock-in Card: Card with current status (Clocked In/Out), clock time, action Button (Clock In / Clock Out)
    - Stats grid: `<div className="portal__grid">` 3-col: hours this week Stat, shifts upcoming Stat, leave balance Stat
    - Upcoming shifts: Card with shift list — each row: date, start time, end time, role Badge
    - Leave section: Card with leave balance + "Request Leave" Button, recent requests table

    **Timeclock Kiosk (MIG-22):**
    - Outer: `<div className="kiosk">` (dark background, full screen)
    - Header: `<div className="kiosk__header">` with brand + `<div className="kiosk__clock">` showing large clock (`<span className="kiosk__time">{HH:MM:SS}</span>` + `<span className="kiosk__date">{Day, DD Month YYYY}</span>`)
    - Stats: `<div className="kiosk__stats">` 4 kiosk stat tiles (Active Staff, Clocked In, Late, On Leave)
    - Title: `<h2 className="kiosk__title">Tap to Clock In/Out</h2>`
    - Staff grid: `<div className="kiosk__grid">` 4-col grid of staff cards. Each card: `<div className="kiosk__card" onClick={handleClock}>` with Avatar, `<div className="kiosk__name">{name}</div>`, `<div className="kiosk__role">{role}</div>`, `<div className="kiosk__state"><span className="kiosk__dot"></span>{status}</div>`
    - Footer: `<div className="kiosk__footer">` with venue name
    - Real-time clock update via setInterval

    **Per D-09:** No changes to actual clock-in/clock-out server actions or onboarding flow logic.
    **CRITICAL PITFALL (Pitfall 6):** Timeclock is public (no auth). Staff Portal has different auth. Do NOT move them into (authenticated)/ route group.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in "src/app/(employee-onboarding)/onboarding/[token]/_components/OnboardingClient.tsx" "src/app/(staff-portal)/portal/_components/PortalClient.tsx" "src/app/(timeclock)/timeclock/_components/TimeclockClient.tsx"; do echo "=== $f ==="; test -f "$f" && echo "EXISTS" || echo "MISSING"; done && grep -c "onboard" "src/app/(employee-onboarding)/onboarding/[token]/_components/OnboardingClient.tsx" 2>/dev/null && grep -c "kiosk" "src/app/(timeclock)/timeclock/_components/TimeclockClient.tsx" 2>/dev/null && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - OnboardingClient.tsx exists with `onboard` layout classes and `Stepper` usage
    - OnboardingClient.tsx contains `FileUpload` usage (RTW step)
    - PortalClient.tsx exists with `portal` layout classes
    - PortalClient.tsx contains `portal__topbar` and `portal__body` classes
    - TimeclockClient.tsx exists with `kiosk` layout classes
    - TimeclockClient.tsx contains `kiosk__clock` and `kiosk__grid` classes
    - None of these files import from AppShell or (authenticated) components
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Onboarding with 6-step wizard + Stepper rail + FileUpload, Staff Portal with clock-in card + shifts + leave, Timeclock Kiosk with dark layout + staff grid + large clock. All using standalone layout CSS classes. No AppShell. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Public Booking, Public Parking, Booking Confirmation, Privacy screens (MIG-23, MIG-24, MIG-25, MIG-26)</name>
  <files>
    src/app/table-booking/page.tsx,
    src/app/table-booking/_components/PublicBookingClient.tsx,
    src/app/parking/guest/[id]/page.tsx,
    src/app/parking/guest/[id]/_components/PublicParkingClient.tsx,
    src/app/booking-confirmation/[token]/page.tsx,
    src/app/booking-confirmation/[token]/_components/BookingConfirmationClient.tsx,
    src/app/privacy/page.tsx
  </files>
  <read_first>
    src/app/table-booking/page.tsx,
    src/app/parking/guest/[id]/page.tsx,
    src/app/booking-confirmation/[token]/page.tsx,
    src/app/privacy/page.tsx,
    src/app/globals.css,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/public-booking.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/public-parking.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/booking-confirmation.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. All 4 screens use the `.public` standalone layout.

    **Public Booking (MIG-23):**
    - Outer: `<div className="public">`
    - Hero: `<div className="public__hero">` with gradient bg, `<div className="public__hero-inner">` with brand mini logo, `<h1 className="public__hero-title">Book a Table</h1>`, `<p className="public__hero-sub">At The Anchor</p>`
    - Main: `<div className="public__main">`
    - Steps breadcrumb: `<div className="public__steps">` with 3 steps: Details, Time, Confirm (active/done/upcoming styling)
    - **Step 1 (Details):** Party size chips (`<button className="public__chip">`) for 1-10+ guests, special occasion Select, notes Textarea
    - **Step 2 (Time):** Date selector (`<div className="public__date">` with date buttons), time slot grid (`<button className="public__slot">`) showing available 30-min slots
    - **Step 3 (Confirm):** Summary card (`<div className="public__summary">`) with booking details, Field+Input for name, email, phone. "Confirm Booking" Button
    - Summary bar at bottom: fixed footer with selected details + "Continue" Button
    - Port existing booking flow logic (server actions, validation, PayPal integration if applicable).

    **Public Parking (MIG-24):**
    - Outer: `<div className="public">`
    - Hero: same pattern as Public Booking but title "Guest Parking"
    - Main: `<div className="public__main">`
    - Form Card: `<div className="public__card">` with Field+Input for arrival date/time (DateTimePicker), departure date/time, vehicle registration, driver name, mobile, email
    - Pricing breakdown: Card showing selected duration, rate, total
    - "Register Parking" Button (primary, full width)
    - Assurance text: `<p className="public__assure">Your details are stored securely</p>`

    **Booking Confirmation (MIG-25):**
    - Outer: `<div className="public">`
    - Hero: slim variant `<div className="public__hero public__hero--slim">`
    - Main: `<div className="public__main">`
    - Success icon: large green checkmark Icon
    - Heading: "Booking Confirmed!"
    - Ticket stub: `<div className="public__ticket">` with QR code placeholder (square gray box or actual QR if existing code generates one), booking reference, date, time, party size, guest name
    - "What happens next" section: numbered steps explaining confirmation email, reminder, cancellation policy
    - "Back to Home" Button

    **Privacy (MIG-26):**
    - Outer: `<div className="public">`
    - Hero: slim variant `<div className="public__hero public__hero--slim">` with title "Privacy Policy"
    - Main: `<div className="public__main public__main--prose">`
    - Article content: `<article className="public__prose">` with headings (h2, h3), paragraphs, lists
    - Port existing privacy policy content as-is — just wrap in new layout classes

    Footer for all public pages: `<div className="public__footer">` with copyright, venue info

    **Per D-09:** No changes to actual booking/parking server actions or payment flows.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in src/app/table-booking/_components/PublicBookingClient.tsx "src/app/parking/guest/[id]/_components/PublicParkingClient.tsx" "src/app/booking-confirmation/[token]/_components/BookingConfirmationClient.tsx" src/app/privacy/page.tsx; do echo "=== $f ==="; test -f "$f" && echo "EXISTS" || echo "MISSING"; done && grep -c "public__hero\|public__main\|className.*public" src/app/table-booking/_components/PublicBookingClient.tsx 2>/dev/null && grep -c "public__ticket" "src/app/booking-confirmation/[token]/_components/BookingConfirmationClient.tsx" 2>/dev/null && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PublicBookingClient.tsx exists with `public__hero`, `public__main`, `public__steps`, `public__chip`, `public__slot` classes
    - PublicParkingClient.tsx exists with `public__hero` and `public__card` classes
    - BookingConfirmationClient.tsx exists with `public__ticket` class
    - privacy/page.tsx contains `public__prose` or `public__main--prose` class
    - None of these files import from AppShell
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Public Booking with hero + 3-step wizard + party chips + time slots, Public Parking with form + pricing, Booking Confirmation with success state + QR ticket stub, Privacy with prose layout. All using .public layout CSS. No AppShell. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -r "ui-v2\|@/components/ui" src/app/auth/ src/app/(employee-onboarding)/ src/app/(staff-portal)/ src/app/(timeclock)/ src/app/table-booking/ src/app/parking/guest/ src/app/booking-confirmation/ src/app/privacy/ src/app/error/ src/app/unauthorized/ --include="*.tsx"` returns zero results
- All 10 standalone-layout screens use their respective CSS layout classes (auth/public/portal/kiosk/onboard)
- None of these screens render inside AppShell
</verification>

<success_criteria>
- Login migrated with auth card, 2FA, SSO using .auth layout
- Onboarding migrated with 6-step wizard using .onboard layout + Stepper
- Staff Portal migrated with clock-in card using .portal layout
- Timeclock Kiosk migrated with dark full-screen using .kiosk layout
- Public Booking migrated with hero + 3-step wizard using .public layout
- Public Parking migrated with form + pricing using .public layout
- Booking Confirmation migrated with QR ticket stub using .public layout
- Privacy migrated with prose layout using .public--slim layout
- Error migrated with reference code card using .auth layout
- Unauthorised migrated with attempted path card using .auth layout
- Zero ui-v2 imports in any migrated page
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-screen-migrations/02-05-SUMMARY.md`
</output>
