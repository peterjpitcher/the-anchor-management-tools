---
phase: 02-screen-migrations
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(authenticated)/messages/page.tsx
  - src/app/(authenticated)/messages/_components/MessagesClient.tsx
  - src/app/(authenticated)/users/page.tsx
  - src/app/(authenticated)/users/_components/UsersClient.tsx
  - src/app/(authenticated)/users/_components/UsersContent.tsx
  - src/app/(authenticated)/users/_components/RolesContent.tsx
  - src/app/(authenticated)/profile/page.tsx
  - src/app/(authenticated)/profile/_components/ProfileClient.tsx
  - src/app/(authenticated)/settings/page.tsx
  - src/app/(authenticated)/settings/_components/SettingsClient.tsx
autonomous: true
requirements:
  - MIG-15
  - MIG-16
  - MIG-17
  - MIG-18

must_haves:
  truths:
    - "User navigates to Messages and sees 3-panel layout (conversation list + thread + contact sidebar) with fixed 560px height"
    - "User navigates to Users and sees user table with roles/permissions matrix view"
    - "User navigates to Profile and sees form cards + avatar sidebar in 2-column layout"
    - "User navigates to Settings and can switch between General, Users, Roles, Profile sub-pages via SectionNav"
    - "Settings embeds UsersContent and RolesContent directly (shared with standalone pages)"
  artifacts:
    - path: "src/app/(authenticated)/messages/_components/MessagesClient.tsx"
      provides: "3-panel messaging layout"
      exports: ["MessagesClient"]
    - path: "src/app/(authenticated)/users/_components/UsersContent.tsx"
      provides: "Reusable users table body (shared by Users page and Settings hub)"
      exports: ["UsersContent"]
    - path: "src/app/(authenticated)/users/_components/RolesContent.tsx"
      provides: "Reusable roles/permissions matrix (shared by standalone and Settings hub)"
      exports: ["RolesContent"]
    - path: "src/app/(authenticated)/settings/_components/SettingsClient.tsx"
      provides: "Settings hub with SectionNav routing to General, Users, Roles, Profile"
      exports: ["SettingsClient"]
  key_links:
    - from: "SettingsClient.tsx"
      to: "UsersContent.tsx, RolesContent.tsx, ProfileClient.tsx"
      via: "SectionNav state-based rendering"
      pattern: "import.*UsersContent.*RolesContent"
    - from: "MessagesClient.tsx"
      to: "560px fixed height 3-column grid"
      via: "CSS grid with fixed height"
      pattern: "grid-cols-\\[320px_1fr_280px\\].*h-\\[560px\\]"
---

<objective>
Migrate communication and settings screens: Messages (HIGH complexity, 3-panel layout), Users (permissions matrix), Profile (form layout), and Settings (hub embedding other pages per Pitfall 3).

Purpose: Messages is one of the most complex layouts (3-panel fixed height). Settings is architecturally important because it embeds Users, Roles, and Profile bodies. Extracting reusable content components prevents duplicate code.
Output: 4 fully migrated screens with reusable content components for Settings hub integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-screen-migrations/02-CONTEXT.md
@.planning/phases/02-screen-migrations/02-RESEARCH.md
@.planning/phases/02-screen-migrations/02-01-SUMMARY.md

<interfaces>
<!-- ds/ components available after Plan 01 -->
Primitives: Button, Badge, Avatar, AvatarStack, Alert, Modal, Skeleton, Empty, Toast, Stat, Input, Select, Textarea, Checkbox, Radio, Switch, Field, ProgressBar, Spinner, SearchInput, Dropdown, DropdownItem, Tooltip, ConfirmDialog, FileUpload, Drawer, Stepper, DateTimePicker, Popover, IconButton
Composites: Card, CardHeader, CardBody, CardFooter, PageHeader, SectionNav, Tabs, Segmented, Table (+ Header/Body/Row/Head/Cell/Pagination), RevenueChart, Sparkline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Messages screen with 3-panel layout (MIG-15)</name>
  <files>
    src/app/(authenticated)/messages/page.tsx,
    src/app/(authenticated)/messages/_components/MessagesClient.tsx
  </files>
  <read_first>
    src/app/(authenticated)/messages/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/messages.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. **CRITICAL PITFALL (from research Pitfall 7):** Messages uses fixed 560px height with overflow:auto on each panel. Height MUST be explicit for overflow to work.

    **Messages (MIG-15):**
    - PageHeader: breadcrumbs [{label:'Messages'}], title "Messages", actions: [New Message button]
    - 3-panel layout: `grid grid-cols-[320px_1fr_280px] h-[560px] gap-0` inside a Card
    - **Left panel (Conversation list):**
      - SearchInput at top for filtering conversations
      - Scrollable list (`overflow-y-auto flex-1`): each conversation row shows Avatar, name, last message preview (truncated), timestamp, unread count Badge
      - Active conversation has `bg-primary-soft` background
      - Click sets selectedConversationId in state
    - **Middle panel (Thread):**
      - Header bar: Avatar + name + status of selected contact
      - Scrollable message area (`overflow-y-auto flex-1`): message bubbles
        - "Us" messages: `ml-auto bg-primary text-primary-fg rounded-2xl rounded-br-sm max-w-[70%] px-4 py-2`
        - "Them" messages: `mr-auto bg-surface-hover text-text rounded-2xl rounded-bl-sm max-w-[70%] px-4 py-2`
        - Each bubble: message text + timestamp below
      - Composer at bottom: `<div className="border-t border-border p-3 flex gap-2">` with Textarea (auto-resize) + Send Button (primary, icon=send)
    - **Right panel (Contact sidebar):**
      - Avatar (large) + name + role/title
      - Detail rows: Phone, Email, Last Active, Member Since — each as label + value
      - Action buttons: Call, Email, Block
      - Border-left separating from middle panel

    **Per D-08:** The handoff shows a static 3-panel view. Keep existing polling/refresh pattern for messages (per research Open Question 4 — no Supabase Realtime).
    **Per D-09:** No changes to server actions.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && grep -c "from '@/ds'" src/app/\(authenticated\)/messages/_components/MessagesClient.tsx && grep -c "ui-v2\|@/components/ui" src/app/\(authenticated\)/messages/_components/MessagesClient.tsx && grep "560\|h-\[560px\]" src/app/\(authenticated\)/messages/_components/MessagesClient.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MessagesClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - MessagesClient.tsx contains `h-[560px]` or `height: 560px` for fixed height panel
    - MessagesClient.tsx contains `grid-cols-[320px_1fr_280px]` or equivalent 3-column layout
    - MessagesClient.tsx contains `overflow` (for scrollable panels)
    - MessagesClient.tsx contains `Textarea` usage (composer)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Messages fully rewritten with 3-panel layout (320px conversation list + 1fr thread + 280px contact sidebar), fixed 560px height, message bubbles with us/them styling, composer with Textarea + Send. All ds/ imports, zero ui-v2. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Users, Profile, Settings with shared content components (MIG-16, MIG-17, MIG-18)</name>
  <files>
    src/app/(authenticated)/users/page.tsx,
    src/app/(authenticated)/users/_components/UsersClient.tsx,
    src/app/(authenticated)/users/_components/UsersContent.tsx,
    src/app/(authenticated)/users/_components/RolesContent.tsx,
    src/app/(authenticated)/profile/page.tsx,
    src/app/(authenticated)/profile/_components/ProfileClient.tsx,
    src/app/(authenticated)/settings/page.tsx,
    src/app/(authenticated)/settings/_components/SettingsClient.tsx
  </files>
  <read_first>
    src/app/(authenticated)/users/page.tsx,
    src/app/(authenticated)/profile/page.tsx,
    src/app/(authenticated)/settings/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/users.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/profile.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/settings.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite entirely. **Per D-05:** ALL sub-pages migrated.
    **CRITICAL PITFALL (from research Pitfall 3):** Settings embeds UsersBody, RolesBody, and ProfileBody inline. Extract body content into reusable components (UsersContent, RolesContent) importable by both the standalone page and the Settings hub.

    **Users (MIG-16):**
    - Create `UsersContent.tsx` — Reusable component (NOT a page):
      - Table with: Avatar + name, email, role Badge (super_admin/manager/staff), department, 2FA status Badge (Enabled/Disabled), last login date, actions Dropdown (Edit/Deactivate/Reset Password)
      - SearchInput + Select for role filter
    - Create `RolesContent.tsx` — Reusable component:
      - Layout: `grid grid-cols-[260px_1fr] gap-6`
      - Left: role list sidebar. Each role as clickable row with name + user count Badge. Active role highlighted.
      - Right: permission matrix. Grid of Checkbox cells: rows = modules (calendar, customers, employees, etc.), columns = actions (view, create, edit, delete). Current permissions checked.
    - `UsersClient.tsx` — Wraps UsersContent with PageHeader and SectionNav (Users / Roles tabs)
    - `page.tsx` — Server component fetching users data, rendering UsersClient

    **Profile (MIG-17):**
    - Layout: `grid grid-cols-[1fr_320px] gap-6`
    - Left column:
      - Personal Details Card: Field + Input for first name, last name, email, phone. Save button.
      - Security Card: rows for Password (change link), 2FA status (Switch toggle), sessions list
      - Notifications Card: Switch toggles for email notifications, SMS notifications, push notifications
    - Right column:
      - Avatar sidebar Card: large Avatar, name, role, "Change Photo" button, account stats (member since, last login)
    - All form Fields use ds/Field wrapper

    **Settings (MIG-18):**
    - SectionNav: 4 items: General, Users, Roles, Profile (per research Open Question 1 — use client-side state within SettingsClient.tsx)
    - useState for activeSection: 'general' | 'users' | 'roles' | 'profile'
    - **General section:**
      - Business Profile Card: logo, name, address, phone, email fields
      - Quick Toggles Card: Switch toggles for online bookings, SMS notifications, auto-confirm, etc.
      - "Other Modes" Card: 2-col grid with FOH mode toggle, kiosk mode toggle
      - 3 settings group Cards in 3-col grid: Booking Settings, Payment Settings, Notification Settings — each with 3-col grids of Field + Input pairs
    - **Users section:** Renders `<UsersContent />` imported from users/_components/
    - **Roles section:** Renders `<RolesContent />` imported from users/_components/
    - **Profile section:** Renders `<ProfileClient />` imported from profile/_components/ (or a ProfileContent.tsx extracted component)

    **Per D-09:** No changes to server actions.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in src/app/\(authenticated\)/users/_components/UsersContent.tsx src/app/\(authenticated\)/users/_components/RolesContent.tsx src/app/\(authenticated\)/profile/_components/ProfileClient.tsx src/app/\(authenticated\)/settings/_components/SettingsClient.tsx; do echo "=== $f ==="; test -f "$f" && echo "EXISTS" || echo "MISSING"; grep -c "from '@/ds'" "$f" 2>/dev/null; grep -c "ui-v2" "$f" 2>/dev/null; done && grep -c "UsersContent\|RolesContent" src/app/\(authenticated\)/settings/_components/SettingsClient.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - UsersContent.tsx exists and contains `from '@/ds'` and zero `ui-v2` imports
    - RolesContent.tsx exists with `grid grid-cols-[260px_1fr]` layout and `Checkbox` usage for permission matrix
    - ProfileClient.tsx contains `from '@/ds'` with `Field` and `Switch` usage
    - SettingsClient.tsx contains `from '@/ds'` and zero `ui-v2` imports
    - SettingsClient.tsx imports `UsersContent` and `RolesContent` (grep returns >= 2 matches)
    - SettingsClient.tsx contains `SectionNav` usage with 4 items
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Users, Profile, Settings fully rewritten. UsersContent and RolesContent are reusable components imported by both standalone pages and Settings hub. Permission matrix renders Checkbox grid. Profile has form cards + avatar sidebar. Settings routes between General/Users/Roles/Profile via SectionNav. All ds/ imports, zero ui-v2. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -r "ui-v2\|@/components/ui" src/app/(authenticated)/messages/ src/app/(authenticated)/users/ src/app/(authenticated)/profile/ src/app/(authenticated)/settings/ --include="*.tsx"` returns zero results
- Messages has 3-panel fixed-height layout
- Settings imports UsersContent and RolesContent from users/_components/
</verification>

<success_criteria>
- Messages migrated with 3-panel layout (320px + 1fr + 280px), 560px fixed height, message bubbles, composer
- Users migrated with reusable UsersContent + RolesContent (permission matrix with Checkbox grid)
- Profile migrated with form cards + avatar sidebar
- Settings migrated as hub embedding Users, Roles, Profile content via SectionNav
- Zero ui-v2 imports in any migrated page
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-screen-migrations/02-04-SUMMARY.md`
</output>
