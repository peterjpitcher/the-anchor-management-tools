---
phase: 02-screen-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ds/primitives/Field.tsx
  - src/ds/primitives/ProgressBar.tsx
  - src/ds/primitives/Spinner.tsx
  - src/ds/primitives/SearchInput.tsx
  - src/ds/primitives/Dropdown.tsx
  - src/ds/primitives/Tooltip.tsx
  - src/ds/primitives/ConfirmDialog.tsx
  - src/ds/primitives/FileUpload.tsx
  - src/ds/primitives/Drawer.tsx
  - src/ds/primitives/Stepper.tsx
  - src/ds/primitives/DateTimePicker.tsx
  - src/ds/primitives/Popover.tsx
  - src/ds/primitives/IconButton.tsx
  - src/ds/primitives/index.ts
  - src/ds/composites/Chart.tsx
  - src/ds/composites/index.ts
  - src/app/globals.css
  - package.json
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - MIG-04

must_haves:
  truths:
    - "Every ui-v2 component used in Phase 2 screens has a ds/ equivalent"
    - "Recharts is installed and a ds/ Chart wrapper exists with brand token styling"
    - "Non-authenticated layout CSS classes (auth, public, portal, kiosk, onboard) exist in globals.css"
    - "All 13 new ds/ primitives are exported from the barrel file"
  artifacts:
    - path: "src/ds/primitives/Field.tsx"
      provides: "Form field wrapper (label + input + error/hint)"
      exports: ["Field"]
    - path: "src/ds/primitives/Dropdown.tsx"
      provides: "Action menu for table row dots buttons"
      exports: ["Dropdown", "DropdownItem"]
    - path: "src/ds/primitives/Drawer.tsx"
      provides: "Slide-in panel via Headless UI Dialog"
      exports: ["Drawer"]
    - path: "src/ds/primitives/FileUpload.tsx"
      provides: "Drag-and-drop file upload dropzone"
      exports: ["FileUpload"]
    - path: "src/ds/composites/Chart.tsx"
      provides: "Recharts wrappers with ds/ brand tokens"
      exports: ["RevenueChart", "Sparkline"]
    - path: "src/app/globals.css"
      provides: "Layout CSS classes for auth, public, portal, kiosk, onboard screens"
      contains: ".auth, .public, .portal, .kiosk, .onboard"
    - path: "package.json"
      provides: "recharts dependency"
      contains: "recharts"
  key_links:
    - from: "src/ds/primitives/index.ts"
      to: "all 13 new primitives"
      via: "named exports"
      pattern: "export.*Field.*Dropdown.*Drawer"
    - from: "src/ds/composites/Chart.tsx"
      to: "recharts"
      via: "import { BarChart } from 'recharts'"
      pattern: "from 'recharts'"
---

<objective>
Build all missing ds/ components (per D-01) and screen-specific CSS classes needed before any screen migration can begin. Then migrate the 4 highest-traffic authenticated screens: Dashboard, Customers, Employees, and Private Bookings (per D-04, pixel-perfect fidelity).

Purpose: Unblocks all subsequent plans by filling the ds/ component gap. Migrating the 4 most-used screens first gives immediate visual impact and validates the migration pattern.
Output: 13 new ds/ primitives, 1 new ds/ composite (Chart), layout CSS classes in globals.css, recharts installed, and 4 fully migrated high-traffic screens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-screen-migrations/02-CONTEXT.md
@.planning/phases/02-screen-migrations/02-RESEARCH.md
@.planning/phases/01-design-system-app-shell/01-02-SUMMARY.md
@.planning/phases/01-design-system-app-shell/01-03-SUMMARY.md

<interfaces>
<!-- Existing ds/ exports the executor must use and extend -->

From src/ds/primitives/index.ts:
```typescript
export { Button } from './Button'
export { Badge } from './Badge'
export { Avatar, AvatarStack } from './Avatar'
export { Alert } from './Alert'
export { Modal } from './Modal'
export { Skeleton } from './Skeleton'
export { Empty } from './Empty'
export { Toast } from './Toast'
export { Stat } from './Stat'
export { Input } from './Input'
export { Select } from './Select'
export { Textarea } from './Textarea'
export { Checkbox } from './Checkbox'
export { Radio } from './Radio'
export { Switch } from './Switch'
```

From src/ds/composites/index.ts:
```typescript
export { Card, CardHeader, CardBody, CardFooter } from './Card'
export { PageHeader } from './PageHeader'
export { SectionNav } from './SectionNav'
export { Tabs } from './Tabs'
export { Segmented } from './Segmented'
export { Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TablePagination } from './Table'
```

From src/ds/tokens/index.ts:
```typescript
export function getToken(name: string): string
export const colors = { brand: {...}, bg, surface, surface2, surfaceHover, border, borderStrong, borderFocus, text, textStrong, textMuted, textSubtle, primary, primaryHover, primarySoft, primarySoftFg, primaryFg, success, successSoft, successFg, warning, warningSoft, warningFg, danger, dangerSoft, dangerFg, info, infoSoft, infoFg } as const
export const spacing = { ... } as const
export const shadows = { ... } as const
export const radii = { ... } as const
```

From src/ds/primitives/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger'
type ButtonSize = 'sm' | 'md' | 'lg'
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant; size?: ButtonSize; icon?: ReactNode; iconRight?: ReactNode; loading?: boolean
}
```

From src/ds/composites/PageHeader.tsx:
```typescript
interface Breadcrumb { label: string; href?: string }
interface PageHeaderProps { breadcrumbs?: Breadcrumb[]; title: string; subtitle?: string; actions?: React.ReactNode; className?: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 13 missing ds/ primitives, Chart composite, install recharts, add layout CSS</name>
  <files>
    src/ds/primitives/Field.tsx,
    src/ds/primitives/ProgressBar.tsx,
    src/ds/primitives/Spinner.tsx,
    src/ds/primitives/SearchInput.tsx,
    src/ds/primitives/Dropdown.tsx,
    src/ds/primitives/Tooltip.tsx,
    src/ds/primitives/ConfirmDialog.tsx,
    src/ds/primitives/FileUpload.tsx,
    src/ds/primitives/Drawer.tsx,
    src/ds/primitives/Stepper.tsx,
    src/ds/primitives/DateTimePicker.tsx,
    src/ds/primitives/Popover.tsx,
    src/ds/primitives/IconButton.tsx,
    src/ds/primitives/index.ts,
    src/ds/composites/Chart.tsx,
    src/ds/composites/index.ts,
    src/app/globals.css,
    package.json
  </files>
  <read_first>
    src/ds/primitives/index.ts,
    src/ds/composites/index.ts,
    src/ds/primitives/Button.tsx,
    src/ds/primitives/Input.tsx,
    src/ds/primitives/Modal.tsx,
    src/ds/composites/Card.tsx,
    src/ds/tokens/index.ts,
    src/app/globals.css,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/ui.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/styles.css
  </read_first>
  <action>
    **Per D-01:** Build ALL missing ds/ components before migrating screens.

    **Install recharts (per D-06):**
    Run `npm install recharts` to add recharts for Dashboard charts.

    **Build 13 new primitives in src/ds/primitives/:**

    1. **Field.tsx** — Form field wrapper. Props: `label: string, error?: string, hint?: string, required?: boolean, children: ReactNode, className?: string`. Renders: `<div>` with `<label className="text-xs font-medium text-text-muted uppercase tracking-wider mb-1.5">`, then children (Input/Select/Textarea slot), then conditional `<p className="text-xs text-danger mt-1">` for error or `<p className="text-xs text-text-subtle mt-1">` for hint. Export `Field`.

    2. **ProgressBar.tsx** — Props: `value: number (0-100), tone?: 'primary'|'success'|'warning'|'danger', size?: 'sm'|'md', className?: string`. Renders: outer `<div className="h-1.5 rounded-full bg-surface-hover">` (sm) or `h-2` (md), inner `<div>` with `style={{ width: '${value}%' }}` and `bg-primary` (or tone-specific bg). Export `ProgressBar`.

    3. **Spinner.tsx** — Props: `size?: 'sm'|'md'|'lg', className?: string`. Renders: `<svg className="animate-spin" viewBox="0 0 24 24">` with a circle path, stroke uses `text-primary`. Sizes: sm=16px, md=20px, lg=24px. Export `Spinner`.

    4. **SearchInput.tsx** — Thin wrapper around ds/Input. Props: `value: string, onChange: (v: string) => void, placeholder?: string, className?: string`. Renders: `<div className="relative">` with `<Input>` that has `icon` prop set to search icon, plus a conditional clear button (X icon) when value is non-empty. Export `SearchInput`.

    5. **Dropdown.tsx** — Use `@headlessui/react` Menu component. Props for Dropdown: `trigger: ReactNode, children: ReactNode, align?: 'left'|'right'`. Renders: `<Menu>` wrapping `<Menu.Button>` (the trigger), `<Menu.Items className="absolute z-50 mt-1 w-48 rounded-lg bg-surface border border-border shadow-lg py-1">`. DropdownItem props: `onClick?: () => void, icon?: ReactNode, danger?: boolean, children: ReactNode`. Renders: `<Menu.Item>` with `<button className="flex w-full items-center gap-2 px-3 py-2 text-sm hover:bg-surface-hover">`. Export `Dropdown, DropdownItem`.

    6. **Tooltip.tsx** — Use Headless UI or a simple title-based approach. Props: `content: string, children: ReactNode, side?: 'top'|'bottom'|'left'|'right'`. For simplicity and performance, use CSS-only tooltip with `data-tooltip` attribute and `::after` pseudo-element, or use a `title` attribute wrapper. If using CSS tooltip: wrap children in `<span className="relative group">` with a `<span className="tooltip">` that shows on group-hover. Export `Tooltip`.

    7. **ConfirmDialog.tsx** — Extends ds/Modal. Props: `open: boolean, onClose: () => void, onConfirm: () => void, title: string, message: string, confirmLabel?: string, cancelLabel?: string, tone?: 'danger'|'warning'`. Renders: `<Modal open={open} onClose={onClose} width="sm">` with title, message paragraph, and footer with Cancel (secondary button) + Confirm (danger/primary button). Export `ConfirmDialog`.

    8. **FileUpload.tsx** — Drag-and-drop dropzone. Props: `accept?: string, maxSize?: number, onFiles: (files: File[]) => void, hint?: string, className?: string`. Renders: `<div>` with dragover/drop/dragleave handlers. Dashed border (`border-2 border-dashed border-border rounded-lg p-6`), centered icon (upload), text "Drag files here or click to browse", hidden `<input type="file">` triggered on click. Active drag state: `border-primary bg-primary-soft`. Export `FileUpload`.

    9. **Drawer.tsx** — Use `@headlessui/react` Dialog. Props: `open: boolean, onClose: () => void, title?: string, children: ReactNode, side?: 'right'|'left', width?: string`. Renders: `<Dialog>` with `<DialogBackdrop className="fixed inset-0 bg-black/30">`, `<DialogPanel className="fixed top-0 right-0 h-full w-[380px] bg-surface shadow-lg">` with slide-in transition (translate-x-full to translate-x-0). Header with title + close button. Scrollable body. Export `Drawer`.

    10. **Stepper.tsx** — Vertical step rail for Onboarding. Props: `steps: { label: string; status: 'done'|'active'|'upcoming' }[], className?: string`. Renders: `<nav>` with `<ol>`, each step as `<li className="flex items-center gap-3 py-2">` with bullet (done=checkmark, active=filled circle, upcoming=empty circle) and label. Export `Stepper`.

    11. **DateTimePicker.tsx** — Styled native input. Props: `type?: 'date'|'time'|'datetime-local', value: string, onChange: (v: string) => void, ...rest`. Renders: `<Input type={type} value={value} onChange={...} />` — delegates to ds/Input with the correct type. This is a thin convenience wrapper. Export `DateTimePicker`.

    12. **Popover.tsx** — Use `@headlessui/react` Popover. Props: `trigger: ReactNode, children: ReactNode, align?: 'left'|'right'`. Renders: `<HeadlessPopover className="relative">` with `<PopoverButton>` and `<PopoverPanel className="absolute z-50 mt-2 w-72 rounded-lg bg-surface border border-border shadow-lg p-4">`. Export `Popover`.

    13. **IconButton.tsx** — Convenience wrapper for Button with icon-only rendering. Props: `icon: ReactNode, label: string, variant?: ButtonVariant, size?: ButtonSize, ...rest`. Renders: `<Button variant={variant} size={size} icon={icon} aria-label={label} className="!px-0 w-[var(--spacing-btn-h)] ..." {...rest} />`. Export `IconButton`.

    **Update src/ds/primitives/index.ts** to add all 13 new exports.

    **Build 1 new composite in src/ds/composites/:**

    **Chart.tsx** — Per D-06, use Recharts wrapped with ds/ tokens.
    - `RevenueChart`: Props `data: { day: string; amount: number }[]`. Uses `<ResponsiveContainer width="100%" height={160}>`, `<BarChart>` with `<XAxis dataKey="day" tick={{ fontSize: 10, fill: 'var(--color-text-subtle)' }} axisLine={false} tickLine={false} />`, `<Bar dataKey="amount" fill="var(--color-primary)" radius={[4,4,2,2]} />`, `<Tooltip>` with ds/ styled content.
    - `Sparkline`: Props `data: number[], color?: string`. Uses `<ResponsiveContainer width={100} height={32}>`, `<AreaChart>` with `<Area dataKey="y" stroke="var(--color-primary)" fill="var(--color-primary)" fillOpacity={0.14} />`.
    Export both from Chart.tsx. Update src/ds/composites/index.ts barrel.

    **Add layout CSS classes to src/app/globals.css:**
    Read the design handoff styles.css file and copy all layout-specific CSS classes into globals.css. These classes define standalone layouts for non-authenticated screens. Must include all classes listed in the research:
    - `.auth` and all `.auth__*` classes (Login, Error, Unauthorised)
    - `.public` and all `.public__*` classes (Public Booking, Public Parking, Booking Confirmation, Privacy)
    - `.portal` and all `.portal__*` classes (Staff Portal)
    - `.kiosk` and all `.kiosk__*` classes (Timeclock)
    - `.onboard` and all `.onboard__*` classes (Onboarding wizard)
    - `.foh-clock` and `.foh-only` and `.foh-only__*` classes (FOH mode)

    Copy the exact CSS from the design handoff styles.css, adjusting any token references to use the @theme CSS variables (e.g., `var(--color-primary)` instead of hardcoded hex). Place these at the end of globals.css after the existing styles.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && grep -c "export.*Field\|export.*ProgressBar\|export.*Spinner\|export.*SearchInput\|export.*Dropdown\|export.*Tooltip\|export.*ConfirmDialog\|export.*FileUpload\|export.*Drawer\|export.*Stepper\|export.*DateTimePicker\|export.*Popover\|export.*IconButton" src/ds/primitives/index.ts && grep -c "RevenueChart\|Sparkline" src/ds/composites/index.ts && grep -c "\.auth\|\.public\|\.portal\|\.kiosk\|\.onboard" src/app/globals.css && grep "recharts" package.json && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/ds/primitives/index.ts contains `export { Field }`, `export { ProgressBar }`, `export { Spinner }`, `export { SearchInput }`, `export { Dropdown, DropdownItem }`, `export { Tooltip }`, `export { ConfirmDialog }`, `export { FileUpload }`, `export { Drawer }`, `export { Stepper }`, `export { DateTimePicker }`, `export { Popover }`, `export { IconButton }`
    - src/ds/composites/index.ts contains `export { RevenueChart, Sparkline }` from Chart
    - src/ds/primitives/Dropdown.tsx contains `import { Menu` from `@headlessui/react`
    - src/ds/primitives/Drawer.tsx contains `import { Dialog` from `@headlessui/react`
    - src/app/globals.css contains `.auth {`, `.public {`, `.portal {`, `.kiosk {`, `.onboard {`
    - package.json contains `"recharts":`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 13 new ds/ primitives exported and building, Chart composite with RevenueChart and Sparkline exported, recharts installed, all 5 layout CSS class groups present in globals.css, npm run build passes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Dashboard screen (MIG-01, pixel-perfect per D-04)</name>
  <files>
    src/app/(authenticated)/dashboard/page.tsx,
    src/app/(authenticated)/dashboard/_components/DashboardClient.tsx
  </files>
  <read_first>
    src/app/(authenticated)/dashboard/page.tsx,
    src/ds/index.ts,
    src/ds/composites/Chart.tsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/dashboard.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite page files entirely. Fresh page.tsx and DashboardClient.tsx matching the design handoff.
    **Per D-04:** Pixel-perfect fidelity for Dashboard.

    Read the existing dashboard/page.tsx to understand current data fetching (server actions, Supabase queries, permission checks). Port ALL data fetching logic to the new page.tsx server component.

    Read the design handoff dashboard.jsx to understand the exact layout. The Dashboard has these sections:
    - **PageHeader** with breadcrumbs: [{label:'Dashboard'}], title: "Dashboard", subtitle with date, action buttons (Export, "View Reports")
    - **4-column stat grid** using ds/Stat: Revenue, Covers, Events, Online Bookings (each with value, delta, icon)
    - **2:1 grid row**: Left = Revenue card with RevenueChart (Recharts bar chart, 14-day data), Right = Today card with today's bookings/events/tasks
    - **Upcoming events table** in a Card with ProgressBar for capacity
    - **Activity feed** in a Card with Avatar + text + timestamp entries
    - **4x MetricMini sparkline cards** at bottom (Covers trend, Revenue trend, Booking rate, Cancellation rate)

    **Write fresh page.tsx:**
    ```typescript
    // Server component: fetch dashboard data, check permissions, render DashboardClient
    import { getSupabaseServerClient } from '@/lib/supabase/server'
    // ... port ALL existing data fetching from current page.tsx
    export default async function DashboardPage() { ... }
    ```

    **Write fresh DashboardClient.tsx:**
    All imports from `@/ds` only. Use: PageHeader, Stat, Card, CardHeader, CardBody, RevenueChart, Sparkline, Table, TableHeader, TableBody, TableRow, TableHead, TableCell, Avatar, Badge, ProgressBar, Icon, Button.

    Layout: `<div className="flex flex-col gap-6">` containing PageHeader, stat grid (`grid grid-cols-4 gap-4`), revenue+today row (`grid grid-cols-3 gap-4` with `col-span-2` on revenue), upcoming events, activity feed, mini metrics row (`grid grid-cols-4 gap-4`).

    **Per D-08:** Match handoff interactions where specified. Preserve existing link/navigation behavior from old dashboard.
    **Per D-09:** Do NOT change any server actions or API calls. UI-only migration.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && grep -c "from '@/ds'" src/app/\(authenticated\)/dashboard/_components/DashboardClient.tsx && grep -c "ui-v2\|@/components/ui" src/app/\(authenticated\)/dashboard/_components/DashboardClient.tsx src/app/\(authenticated\)/dashboard/page.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(authenticated)/dashboard/_components/DashboardClient.tsx contains `from '@/ds'` (at least 1 import line)
    - src/app/(authenticated)/dashboard/_components/DashboardClient.tsx does NOT contain `ui-v2` or `@/components/ui` (grep returns 0)
    - src/app/(authenticated)/dashboard/page.tsx does NOT contain `ui-v2` or `@/components/ui`
    - DashboardClient.tsx contains `RevenueChart` usage
    - DashboardClient.tsx contains `Sparkline` usage
    - DashboardClient.tsx contains `PageHeader` usage
    - DashboardClient.tsx contains `Stat` usage
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Dashboard page fully rewritten with ds/ imports only, RevenueChart bar chart rendering, Sparkline mini metrics, stat grid, today card, upcoming events, activity feed -- all matching design handoff. No ui-v2 imports. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Customers, Employees, Private Bookings screens (MIG-02, MIG-03, MIG-04, pixel-perfect per D-04)</name>
  <files>
    src/app/(authenticated)/customers/page.tsx,
    src/app/(authenticated)/customers/_components/CustomersClient.tsx,
    src/app/(authenticated)/employees/page.tsx,
    src/app/(authenticated)/employees/_components/EmployeesClient.tsx,
    src/app/(authenticated)/private-bookings/page.tsx,
    src/app/(authenticated)/private-bookings/_components/PrivateBookingsClient.tsx
  </files>
  <read_first>
    src/app/(authenticated)/customers/page.tsx,
    src/app/(authenticated)/employees/page.tsx,
    src/app/(authenticated)/private-bookings/page.tsx,
    src/ds/index.ts,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/customers.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/employees.jsx,
    /Users/peterpitcher/Library/Mobile Documents/com~apple~CloudDocs/Downloads/ams/project/private-bookings.jsx
  </read_first>
  <action>
    **Per D-07:** Rewrite all 3 screens entirely. Fresh page.tsx and *Client.tsx files for each.
    **Per D-04:** Pixel-perfect fidelity for Customers, Employees, Private Bookings.

    For each screen: read existing page.tsx to extract data fetching + permissions. Read design handoff JSX for exact layout. Write fresh files.

    **Customers (MIG-02):**
    - PageHeader: breadcrumbs [{label:'Customers'}], title "Customers", subtitle "1,284 customers - 412 active in last 90 days", actions: [New Customer button, Export button]
    - Stat grid: 4-col stats (Total, Active, VIP, New This Month)
    - Tabs: All / VIP / Flagged / Recent
    - Card with: search/filter bar row (SearchInput + Select for label filter + "More Filters" button), table with Checkbox column for bulk selection, Avatar+name, email, phone, last visit date, spend total, label Badges. TablePagination footer.
    - Bulk action bar (appears when rows selected): "N selected" + SMS button + Email button

    **Employees (MIG-03):**
    - PageHeader: breadcrumbs [{label:'Employees'}], title "Employees", subtitle with count, actions: [Add Employee button]
    - Master-detail layout: `grid grid-cols-[1fr_380px] gap-6`
    - Left: Card with search bar + employee table (Avatar+name, role Badge, department, start date, status Badge)
    - Right: Detail panel Card (shown when row clicked) with large Avatar, name, role, contact details, notes section, action buttons (Edit, Deactivate)
    - Click on table row updates the detail panel (useState for selectedEmployeeId)

    **Private Bookings (MIG-04):**
    - PageHeader: breadcrumbs [{label:'Private Bookings'}], title "Private Bookings", subtitle with count, actions: [New Booking button]
    - Tabs: Upcoming / Past / Cancelled
    - Card with: search + date filter row, table with date, customer name (Avatar), event type, guest count, deposit status Badge (Paid/Unpaid/Partial), total, actions Dropdown
    - Run sheet card: when a booking row is clicked, shows a Card below the table with 3-col grid detail (date/time, customer details, catering/setup notes, payment breakdown)

    **Per D-09:** All server action calls and data fetching ported from existing code. No changes to actual API calls.
    **All imports from `@/ds` only.** No ui-v2 imports.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && for f in src/app/\(authenticated\)/customers/_components/CustomersClient.tsx src/app/\(authenticated\)/employees/_components/EmployeesClient.tsx src/app/\(authenticated\)/private-bookings/_components/PrivateBookingsClient.tsx; do echo "=== $f ==="; grep -c "from '@/ds'" "$f"; grep -c "ui-v2\|@/components/ui" "$f"; done && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CustomersClient.tsx contains `from '@/ds'` imports and zero `ui-v2` or `@/components/ui` imports
    - EmployeesClient.tsx contains `from '@/ds'` imports and zero `ui-v2` or `@/components/ui` imports
    - PrivateBookingsClient.tsx contains `from '@/ds'` imports and zero `ui-v2` or `@/components/ui` imports
    - CustomersClient.tsx contains `Checkbox` usage (bulk selection)
    - CustomersClient.tsx contains `SearchInput` usage
    - EmployeesClient.tsx contains `grid grid-cols-[1fr_380px]` or equivalent master-detail layout
    - PrivateBookingsClient.tsx contains `Tabs` usage
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Customers, Employees, and Private Bookings all rewritten with ds/ imports only, matching design handoff layouts pixel-perfectly. Bulk selection works on Customers, master-detail works on Employees, run sheet card works on Private Bookings. No ui-v2 imports. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -r "ui-v2\|@/components/ui" src/app/(authenticated)/dashboard/ src/app/(authenticated)/customers/ src/app/(authenticated)/employees/ src/app/(authenticated)/private-bookings/ --include="*.tsx"` returns zero results
- All 13 new ds/ primitives importable: `grep -c "export" src/ds/primitives/index.ts` returns 28+ (15 existing + 13 new)
- `grep "recharts" package.json` shows recharts is installed
- `grep -c "\.auth\|\.public\|\.portal\|\.kiosk\|\.onboard" src/app/globals.css` returns 5+ matches
</verification>

<success_criteria>
- All 13 missing ds/ components built and exported from barrel
- Recharts installed and Chart composite available
- All 5 standalone layout CSS class groups in globals.css
- Dashboard, Customers, Employees, Private Bookings fully migrated to ds/ components
- Zero ui-v2 imports in any migrated page
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-screen-migrations/02-01-SUMMARY.md`
</output>
