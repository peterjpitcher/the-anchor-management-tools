---
phase: 03-new-sections
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/(authenticated)/settings/design-system/page.tsx
  - src/app/(authenticated)/settings/page.tsx
  - src/app/(authenticated)/performers/page.tsx
autonomous: true
requirements:
  - MODE-02
  - NEW-05

must_haves:
  truths:
    - "User navigates to /settings/design-system and sees a single scrollable page with colour swatches, typography scale, spacing, icons grid, and live component examples"
    - "User accesses Design System page via a link on the /settings page — not via sidebar navigation"
    - "User cannot navigate to /performers — route shows redirect or 404"
    - "Design System page uses anchor links for internal navigation, not SectionNav sub-routes per D-20"
  artifacts:
    - path: "src/app/(authenticated)/settings/design-system/page.tsx"
      provides: "Design System documentation page"
      contains: "import"
    - path: "src/app/(authenticated)/settings/page.tsx"
      provides: "Settings hub with Design System link added"
      contains: "design-system"
  key_links:
    - from: "src/app/(authenticated)/settings/page.tsx"
      to: "src/app/(authenticated)/settings/design-system/page.tsx"
      via: "Link href to /settings/design-system"
      pattern: "design-system"
    - from: "src/app/(authenticated)/settings/design-system/page.tsx"
      to: "src/ds/index.ts"
      via: "imports all ds/ components for live preview"
      pattern: "import.*from.*@/ds"
---

<objective>
Build the Design System documentation page under /settings, add its link to the Settings hub, and remove the Performers route.

Purpose: The Design System page serves as an internal developer/admin reference for all ds/ components (per D-19, D-20, D-21). Performers removal completes D-01. This plan depends on Plan 01 which removes Performers from SidebarNav.
Output: Live Design System documentation page, Settings hub with DS link, Performers route deleted.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-new-sections/03-CONTEXT.md
@.planning/phases/03-new-sections/03-RESEARCH.md

@src/ds/index.ts
@src/ds/tokens.ts
@src/ds/icons/index.ts
@src/app/(authenticated)/settings/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/ds/tokens.ts (design token values for swatches):
```typescript
// Contains color palette, shadows, radii, spacing exports
// Exact exports need to be read from file — includes brand colors 50-900, semantic surfaces, status colors
```

From src/ds/primitives/index.ts:
```typescript
export { Button, Badge, Avatar, AvatarStack, Alert, Modal, Skeleton, Empty, Toast, toast,
  Stat, Input, Select, Textarea, Checkbox, Radio, Switch, Field, ProgressBar, Spinner,
  SearchInput, Dropdown, DropdownItem, Tooltip, ConfirmDialog, FileUpload, Drawer,
  Stepper, DateTimePicker, Popover, IconButton }
```

From src/ds/composites/index.ts:
```typescript
export { Card, CardHeader, CardBody, CardFooter, PageHeader, SectionNav, Tabs, Segmented,
  Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TablePagination,
  RevenueChart, Sparkline }
```

From src/ds/icons:
```typescript
// 47+ icons exported as named exports
// Icon component: <Icon name="iconName" size={16} className="..." />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design System documentation page and Settings hub link</name>
  <files>
    src/app/(authenticated)/settings/design-system/page.tsx,
    src/app/(authenticated)/settings/page.tsx
  </files>
  <read_first>
    src/app/(authenticated)/settings/page.tsx,
    src/ds/tokens.ts,
    src/ds/index.ts,
    src/ds/primitives/index.ts,
    src/ds/composites/index.ts,
    src/ds/icons/index.ts,
    src/ds/icons/paths.tsx
  </read_first>
  <action>
    **Settings page.tsx — add Design System link (per D-21):**
    - Read the existing settings page to understand its structure
    - Add a new card/link for "Design System" in the settings hub, pointing to `/settings/design-system`
    - Use a Card or a styled link matching the existing settings page pattern
    - Place it in a logical position (e.g., at the end of the settings groups, under a "Developer Tools" or "System" section)
    - Icon: `palette`
    - Description: "Component library, colours, typography, and spacing reference"

    **Design System page — settings/design-system/page.tsx (per D-19, D-20, D-22):**
    - This is a server component — no interactivity needed (static showcase per research recommendation)
    - Single scrollable page with anchor links at the top for navigation per D-20
    - DO NOT use SectionNav sub-routes per D-20

    Page structure with sections:

    **1. Sticky anchor navigation bar at top:**
    - Horizontal list of anchor links: Colours, Typography, Spacing, Icons, Buttons, Badges, Avatars, Alerts, Cards, Tables, Form Controls, Modals, Navigation, Data Display
    - Use `id` attributes on each section heading for anchor targets

    **2. Colours section (id="colours"):**
    - Import color tokens from `@/ds/tokens`
    - Brand palette: render swatches for brand-50 through brand-900 as colored div boxes with hex value labels
    - Semantic colors: surface, border, text-primary, text-secondary, text-muted, text-inverted
    - Status colors: success, warning, danger, info — each as a swatch row
    - Each swatch: `w-16 h-16 rounded-lg` div with `style={{ backgroundColor: tokenValue }}` + label below

    **3. Typography section (id="typography"):**
    - Display heading levels: h1 through h6 with their actual rendered sizes
    - Body text: regular, medium, semibold
    - Mono text: JetBrains Mono sample
    - Show actual class names: `text-2xl font-semibold`, `text-sm font-mono`, etc.

    **4. Spacing section (id="spacing"):**
    - Visual spacing scale: render rectangles showing 0.5, 1, 2, 3, 4, 6, 8, 12, 16 spacing units
    - Each as a colored bar with px label

    **5. Icons section (id="icons"):**
    - Import all icon names from `@/ds/icons`
    - Render a grid of all 47+ icons: each in a card-like cell showing the Icon component + name label below
    - Grid: `grid grid-cols-6 sm:grid-cols-8 md:grid-cols-10 lg:grid-cols-12 gap-4`
    - Each cell: centered Icon at 24px + text-xs name below

    **6. Buttons section (id="buttons"):**
    - Row of Button variants: primary, secondary, ghost, danger — each labeled
    - Row of Button sizes: sm, md, lg
    - Row showing: with icon, loading state, disabled state
    - All rendered as actual `<Button>` components from ds/

    **7. Badges section (id="badges"):**
    - Row of Badge tones: neutral, primary, success, warning, danger, info — each labeled
    - Row with dot variant: same tones with `dot` prop

    **8. Avatars section (id="avatars"):**
    - Row of Avatar sizes: xs, sm, md, lg — with sample names
    - AvatarStack example with 5 avatars

    **9. Alerts section (id="alerts"):**
    - Stack of Alert tones: info, success, warning, danger — each with title and body text

    **10. Cards section (id="cards"):**
    - Example Card with CardHeader (title + subtitle + action button), CardBody (sample content), CardFooter

    **11. Tables section (id="tables"):**
    - Example Table with 3 columns, 3 rows of sample data, TablePagination

    **12. Form Controls section (id="form-controls"):**
    - Grid showing each form control: Input (text, with icon), Select (with options), Textarea, Checkbox (checked + unchecked), Radio (selected + unselected), Switch (on + off), Field with label and helper text, SearchInput, DateTimePicker, FileUpload

    **13. Modals & Drawers section (id="modals"):**
    - Static screenshot/description of Modal and Drawer — do NOT render open modals on the page
    - Show code example text for how to use Modal and Drawer

    **14. Navigation section (id="navigation"):**
    - Example PageHeader with breadcrumbs
    - Example SectionNav with 3 items
    - Example Tabs with 3 tabs
    - Example Segmented with 3 options

    **15. Data Display section (id="data-display"):**
    - Row of Stat tiles with sample data
    - ProgressBar at various values (25%, 50%, 75%, 100%)
    - Skeleton loading states
    - Empty state component
    - Spinner
    - Tooltip wrapping a Button
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && npx tsc --noEmit 2>&1 | head -30 && test -f src/app/\(authenticated\)/settings/design-system/page.tsx && echo "ds page exists" && grep -c "design-system" src/app/\(authenticated\)/settings/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(authenticated)/settings/design-system/page.tsx` exists and contains `import` from `@/ds`
    - `src/app/(authenticated)/settings/design-system/page.tsx` contains `id="colours"` and `id="typography"` and `id="icons"` and `id="buttons"` and `id="form-controls"`
    - `src/app/(authenticated)/settings/design-system/page.tsx` contains `Button` and `Badge` and `Avatar` and `Alert` and `Card` and `Table` and `Input` and `Stat` (live component renders)
    - `src/app/(authenticated)/settings/design-system/page.tsx` does NOT contain `SectionNav` with href routing (per D-20 — anchor links only)
    - `src/app/(authenticated)/settings/page.tsx` contains `design-system` as a link/href
    - `npx tsc --noEmit` completes with no errors related to settings/design-system
  </acceptance_criteria>
  <done>Design System documentation page renders all ds/ components as live previews with colour swatches, typography scale, spacing, icons grid, and anchor navigation. Settings hub links to it per D-21.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Performers route and clean up references</name>
  <files>
    src/app/(authenticated)/performers/page.tsx,
    src/app/(authenticated)/performers/[id]/page.tsx,
    src/app/(authenticated)/performers/loading.tsx
  </files>
  <read_first>
    src/app/(authenticated)/performers/page.tsx,
    src/app/(authenticated)/performers/[id]/page.tsx,
    src/app/(authenticated)/performers/loading.tsx,
    src/ds/shell/SidebarNav.tsx
  </read_first>
  <action>
    **Per D-01: Remove Performers section entirely.**

    **Delete the performers route directory:**
    - Delete `src/app/(authenticated)/performers/` directory and all its contents:
      - `page.tsx`
      - `loading.tsx`
      - `[id]/` subdirectory (contains at least page.tsx)
    - Use `rm -rf src/app/(authenticated)/performers/`

    **Verify SidebarNav cleanup:**
    - Confirm that Plan 01 Task 1 already removed the performers entry from `src/ds/shell/SidebarNav.tsx` NAV_GROUPS
    - If NOT already removed (e.g., Plan 01 hasn't executed yet), remove line 27: `{ id: 'performers', label: 'Performers', icon: 'mic', href: '/performers' }`

    **Check for other references:**
    - Search for any imports or links to `/performers` in the codebase: `grep -rn "performers" src/app/ src/components/ src/ds/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v "actions/performer"` 
    - Note: the existing `performer_submissions` action file stays per D-01 ("can remain but is not wired to any UI")
    - Remove any remaining route references, Link components, or navigation items pointing to /performers
    - Do NOT delete `src/app/actions/performer-submissions.ts` or similar action files — only the route/page files
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && test ! -d src/app/\(authenticated\)/performers && echo "performers dir deleted" || echo "performers dir still exists" && grep -rn "href.*performers\|to.*performers" src/app/ src/ds/ --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v "actions/" | grep -v "node_modules" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/app/(authenticated)/performers/` does NOT exist
    - `grep -rn "href.*performers" src/app/ src/ds/ --include="*.tsx" | grep -v actions/` returns 0 lines
    - `grep "performers" src/ds/shell/SidebarNav.tsx` returns 0 lines
    - Files in `src/app/actions/` related to performers are preserved (not deleted per D-01)
    - `npx tsc --noEmit` completes with no errors
  </acceptance_criteria>
  <done>Performers route directory deleted, no navigation references remain, action files preserved per D-01.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run lint` passes with no warnings
- /settings/design-system renders all component previews and token swatches
- /settings page has a link to Design System page
- /performers route no longer exists (404)
- No sidebar navigation entry for Performers or Design System (handled by Plan 01)
</verification>

<success_criteria>
Design System documentation page is live at /settings/design-system with full component showcases, colour palette, typography, icons grid, and anchor navigation. Performers route is completely removed. Settings hub links to Design System page.
</success_criteria>

<output>
After completion, create `.planning/phases/03-new-sections/03-04-SUMMARY.md`
</output>
