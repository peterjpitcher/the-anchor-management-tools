---
phase: 04-modes-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  # Gap components (new ds/ components)
  - src/ds/composites/PageLayout.tsx
  - src/ds/composites/Section.tsx
  - src/ds/primitives/LinkButton.tsx
  - src/ds/primitives/Accordion.tsx
  - src/ds/primitives/Pagination.tsx
  - src/ds/composites/DataTable.tsx
  - src/ds/composites/index.ts
  - src/ds/primitives/index.ts
  - src/lib/format.ts
  # Settings area (28 files)
  - src/app/(authenticated)/settings/api-keys/ApiKeysManager.tsx
  - src/app/(authenticated)/settings/api-keys/page.tsx
  - src/app/(authenticated)/settings/audit-logs/AuditLogsClient.tsx
  - src/app/(authenticated)/settings/background-jobs/BackgroundJobsClient.tsx
  - src/app/(authenticated)/settings/budgets/BudgetsManager.tsx
  - src/app/(authenticated)/settings/budgets/page.tsx
  - src/app/(authenticated)/settings/business-hours/BusinessHoursManager.tsx
  - src/app/(authenticated)/settings/business-hours/SpecialHoursCalendar.tsx
  - src/app/(authenticated)/settings/business-hours/SpecialHoursClientWrapper.tsx
  - src/app/(authenticated)/settings/business-hours/SpecialHoursModal.tsx
  - src/app/(authenticated)/settings/business-hours/page.tsx
  - src/app/(authenticated)/settings/calendar-notes/CalendarNotesManager.tsx
  - src/app/(authenticated)/settings/calendar-notes/page.tsx
  - src/app/(authenticated)/settings/categories/CategoriesClient.tsx
  - src/app/(authenticated)/settings/customer-labels/CustomerLabelsClient.tsx
  - src/app/(authenticated)/settings/customer-labels/page.tsx
  - src/app/(authenticated)/settings/event-categories/page.tsx
  - src/app/(authenticated)/settings/gdpr/page.tsx
  - src/app/(authenticated)/settings/import-messages/ImportMessagesClient.tsx
  - src/app/(authenticated)/settings/menu-target/MenuTargetForm.tsx
  - src/app/(authenticated)/settings/menu-target/page.tsx
  - src/app/(authenticated)/settings/message-templates/MessageTemplatesClient.tsx
  - src/app/(authenticated)/settings/pay-bands/PayBandsManager.tsx
  - src/app/(authenticated)/settings/pay-bands/page.tsx
  - src/app/(authenticated)/settings/rota/RotaSettingsManager.tsx
  - src/app/(authenticated)/settings/rota/page.tsx
  - src/app/(authenticated)/settings/table-bookings/TableSetupManager.tsx
  - src/app/(authenticated)/settings/table-bookings/page.tsx
  # Private bookings (14 files)
  - src/app/(authenticated)/private-bookings/PrivateBookingsClient.tsx
  - src/app/(authenticated)/private-bookings/[id]/PaymentHistoryTable.tsx
  - src/app/(authenticated)/private-bookings/[id]/PrivateBookingDetailClient.tsx
  - src/app/(authenticated)/private-bookings/[id]/communications/page.tsx
  - src/app/(authenticated)/private-bookings/[id]/edit/page.tsx
  - src/app/(authenticated)/private-bookings/[id]/items/page.tsx
  - src/app/(authenticated)/private-bookings/[id]/messages/PrivateBookingMessagesClient.tsx
  - src/app/(authenticated)/private-bookings/calendar/page.tsx
  - src/app/(authenticated)/private-bookings/new/page.tsx
  - src/app/(authenticated)/private-bookings/settings/catering/page.tsx
  - src/app/(authenticated)/private-bookings/settings/page.tsx
  - src/app/(authenticated)/private-bookings/settings/spaces/page.tsx
  - src/app/(authenticated)/private-bookings/settings/vendors/page.tsx
  - src/app/(authenticated)/private-bookings/sms-queue/page.tsx
  # Employees (5 files)
  - src/app/(authenticated)/employees/EmployeesClientPage.tsx
  - src/app/(authenticated)/employees/[employee_id]/edit/EmployeeEditClient.tsx
  - src/app/(authenticated)/employees/[employee_id]/page.tsx
  - src/app/(authenticated)/employees/birthdays/page.tsx
  - src/app/(authenticated)/employees/new/NewEmployeeOnboardingClient.tsx
  # Invoices + Quotes (18 files)
  - src/app/(authenticated)/invoices/InvoicesClient.tsx
  - src/app/(authenticated)/invoices/MobileInvoiceCard.tsx
  - src/app/(authenticated)/invoices/[id]/InvoiceDetailClient.tsx
  - src/app/(authenticated)/invoices/[id]/edit/page.tsx
  - src/app/(authenticated)/invoices/[id]/payment/page.tsx
  - src/app/(authenticated)/invoices/catalog/page.tsx
  - src/app/(authenticated)/invoices/export/page.tsx
  - src/app/(authenticated)/invoices/new/page.tsx
  - src/app/(authenticated)/invoices/recurring/[id]/edit/page.tsx
  - src/app/(authenticated)/invoices/recurring/[id]/page.tsx
  - src/app/(authenticated)/invoices/recurring/new/page.tsx
  - src/app/(authenticated)/invoices/recurring/page.tsx
  - src/app/(authenticated)/invoices/vendors/page.tsx
  - src/app/(authenticated)/quotes/QuotesClient.tsx
  - src/app/(authenticated)/quotes/[id]/convert/page.tsx
  - src/app/(authenticated)/quotes/[id]/edit/page.tsx
  - src/app/(authenticated)/quotes/[id]/page.tsx
  - src/app/(authenticated)/quotes/new/page.tsx
  # Rota (15 files)
  - src/app/(authenticated)/rota/AddShiftsModal.tsx
  - src/app/(authenticated)/rota/BookHolidayModal.tsx
  - src/app/(authenticated)/rota/CreateShiftModal.tsx
  - src/app/(authenticated)/rota/RotaGrid.tsx
  - src/app/(authenticated)/rota/ShiftDetailModal.tsx
  - src/app/(authenticated)/rota/dashboard/page.tsx
  - src/app/(authenticated)/rota/leave/LeaveManagerClient.tsx
  - src/app/(authenticated)/rota/leave/page.tsx
  - src/app/(authenticated)/rota/page.tsx
  - src/app/(authenticated)/rota/payroll/PayrollClient.tsx
  - src/app/(authenticated)/rota/payroll/page.tsx
  - src/app/(authenticated)/rota/templates/ShiftTemplatesManager.tsx
  - src/app/(authenticated)/rota/templates/page.tsx
  - src/app/(authenticated)/rota/timeclock/TimeclockManager.tsx
  - src/app/(authenticated)/rota/timeclock/page.tsx
  # Menu management (16 files)
  - src/app/(authenticated)/menu-management/_components/MenuDishesTable.tsx
  - src/app/(authenticated)/menu-management/dishes/_components/CompositionRow.tsx
  - src/app/(authenticated)/menu-management/dishes/_components/DishCompositionTab.tsx
  - src/app/(authenticated)/menu-management/dishes/_components/DishDrawer.tsx
  - src/app/(authenticated)/menu-management/dishes/_components/DishExpandedRow.tsx
  - src/app/(authenticated)/menu-management/dishes/_components/DishMenusTab.tsx
  - src/app/(authenticated)/menu-management/dishes/_components/DishOverviewTab.tsx
  - src/app/(authenticated)/menu-management/dishes/page.tsx
  - src/app/(authenticated)/menu-management/ingredients/_components/IngredientDrawer.tsx
  - src/app/(authenticated)/menu-management/ingredients/_components/IngredientExpandedRow.tsx
  - src/app/(authenticated)/menu-management/ingredients/_components/PriceHistoryPopover.tsx
  - src/app/(authenticated)/menu-management/ingredients/page.tsx
  - src/app/(authenticated)/menu-management/recipes/_components/RecipeDrawer.tsx
  - src/app/(authenticated)/menu-management/recipes/_components/RecipeExpandedRow.tsx
  - src/app/(authenticated)/menu-management/recipes/_components/RecipeIngredientRow.tsx
  - src/app/(authenticated)/menu-management/recipes/page.tsx
  # Receipts (8 files)
  - src/app/(authenticated)/receipts/_components/ui/ReceiptRules.tsx
  - src/app/(authenticated)/receipts/bulk/page.tsx
  - src/app/(authenticated)/receipts/missing-expense/page.tsx
  - src/app/(authenticated)/receipts/monthly/MonthlyCharts.tsx
  - src/app/(authenticated)/receipts/monthly/page.tsx
  - src/app/(authenticated)/receipts/pnl/page.tsx
  - src/app/(authenticated)/receipts/vendors/_components/VendorSummaryGrid.tsx
  - src/app/(authenticated)/receipts/vendors/page.tsx
  # Remaining authenticated areas (29 files)
  - src/app/(authenticated)/customers/CustomersClient.tsx
  - src/app/(authenticated)/customers/[id]/page.tsx
  - src/app/(authenticated)/customers/insights/page.tsx
  - src/app/(authenticated)/dashboard/UpcomingScheduleCalendar.tsx
  - src/app/(authenticated)/expenses/insights/_components/ExpensesInsightsClient.tsx
  - src/app/(authenticated)/expenses/insights/page.tsx
  - src/app/(authenticated)/messages/bulk/BulkMessagesClient.tsx
  - src/app/(authenticated)/mgd/insights/_components/MgdInsightsClient.tsx
  - src/app/(authenticated)/mgd/insights/page.tsx
  - src/app/(authenticated)/mileage/destinations/page.tsx
  - src/app/(authenticated)/mileage/insights/_components/MileageInsightsClient.tsx
  - src/app/(authenticated)/mileage/insights/page.tsx
  - src/app/(authenticated)/parking/ParkingClient.tsx
  - src/app/(authenticated)/profile/change-password/page.tsx
  - src/app/(authenticated)/roles/components/RoleCard.tsx
  - src/app/(authenticated)/roles/components/RolePermissionsModal.tsx
  - src/app/(authenticated)/roles/new/page.tsx
  - src/app/(authenticated)/roles/page.tsx
  - src/app/(authenticated)/table-bookings/[id]/BookingDetailClient.tsx
  - src/app/(authenticated)/table-bookings/[id]/PreorderTab.tsx
  - src/app/(authenticated)/table-bookings/[id]/page.tsx
  - src/app/(authenticated)/table-bookings/boh/BohBookingsClient.tsx
  - src/app/(authenticated)/table-bookings/boh/page.tsx
  - src/app/(authenticated)/table-bookings/foh/FohClockWidget.tsx
  - src/app/(authenticated)/table-bookings/foh/components/FohBookingDetailModal.tsx
  - src/app/(authenticated)/table-bookings/foh/components/FohCreateBookingModal.tsx
  - src/app/(authenticated)/table-bookings/foh/components/FohMiniModals.tsx
  - src/app/(authenticated)/table-bookings/foh/page.tsx
  - src/app/(authenticated)/table-bookings/reports/page.tsx
  # Auth + Staff Portal + Public (6 files)
  - src/app/(staff-portal)/portal/leave/LeaveRequestForm.tsx
  - src/app/(staff-portal)/portal/leave/page.tsx
  - src/app/auth/login/page-client.tsx
  - src/app/auth/reset-password/page.tsx
  - src/app/auth/reset/reset-password-form.tsx
  - src/app/booking-portal/[token]/page.tsx
  # Shared components (43 files)
  - src/components/events/command-center/CommandCenterShell.tsx
  - src/components/events/command-center/EventCalendarView.tsx
  - src/components/events/command-center/EventCard.tsx
  - src/components/events/command-center/EventExportPanel.tsx
  - src/components/events/command-center/TaskSidebar.tsx
  - src/components/features/catering/CateringManager.tsx
  - src/components/features/catering/CateringPackageCard.tsx
  - src/components/features/catering/CateringPackageModal.tsx
  - src/components/features/customers/CustomerForm.tsx
  - src/components/features/customers/CustomerImport.tsx
  - src/components/features/customers/WinBackCampaign.tsx
  - src/components/features/employees/AddEmployeeAttachmentForm.tsx
  - src/components/features/employees/AddEmployeeNoteForm.tsx
  - src/components/features/employees/EmergencyContactsTab.tsx
  - src/components/features/employees/EmployeeAttachmentsList.tsx
  - src/components/features/employees/EmployeeForm.tsx
  - src/components/features/employees/EmployeeHolidaysTab.tsx
  - src/components/features/employees/EmployeePayTab.tsx
  - src/components/features/employees/EmployeeStatusActions.tsx
  - src/components/features/employees/FinancialDetailsForm.tsx
  - src/components/features/employees/HealthRecordsForm.tsx
  - src/components/features/employees/InviteEmployeeModal.tsx
  - src/components/features/employees/OnboardingChecklistTab.tsx
  - src/components/features/employees/RightToWorkTab.tsx
  - src/components/features/employees/SendBirthdayRemindersButton.tsx
  - src/components/features/events/EventCategoryFormGrouped.tsx
  - src/components/features/events/EventChecklistCard.tsx
  - src/components/features/events/EventFormGrouped.tsx
  - src/components/features/events/EventImageUpload.tsx
  - src/components/features/events/EventMarketingLinksCard.tsx
  - src/components/features/events/EventPromotionContentCard.tsx
  - src/components/features/invoices/EmailInvoiceModal.tsx
  - src/components/features/menu/SmartImportModal.tsx
  - src/components/features/shared/AppNavigation.tsx
  - src/components/features/shared/SquareImageUpload.tsx
  - src/components/modals/AddNoteModal.tsx
  - src/components/modals/ChasePaymentModal.tsx
  - src/components/modals/EmailQuoteModal.tsx
  - src/components/private-bookings/CalendarView.tsx
  - src/components/private-bookings/CommunicationsTab.tsx
  - src/components/private-bookings/DeleteBookingButton.tsx
  - src/components/private-bookings/SmsQueueActionForm.tsx
  - src/components/schedule-calendar/ScheduleCalendar.tsx
  # SortableHeader migration (5 files from ui/)
  - src/app/(authenticated)/expenses/_components/ExpensesClient.tsx
  - src/app/(authenticated)/expenses/insights/_components/ExpensesInsightsClient.tsx
  - src/app/(authenticated)/mgd/_components/MgdClient.tsx
  - src/app/(authenticated)/mileage/_components/MileageClient.tsx
  - src/app/(authenticated)/mileage/insights/_components/MileageInsightsClient.tsx
  # Refund components (move)
  - src/components/features/invoices/RefundDialog.tsx
  - src/components/features/invoices/RefundHistoryTable.tsx
  # Legacy directory deletion
  - src/components/ui-v2/ (DELETE)
  - src/components/ui/ (DELETE)
  # Documentation
  - CLAUDE.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04]

must_haves:
  truths:
    - "Zero files in src/ import from @/components/ui-v2"
    - "Zero files in src/ import from @/components/ui/"
    - "The directory src/components/ui-v2/ does not exist"
    - "The directory src/components/ui/ does not exist"
    - "npm run build succeeds with zero errors"
    - "CLAUDE.md references ds/ as the canonical component system"
  artifacts:
    - path: "src/ds/composites/PageLayout.tsx"
      provides: "Page wrapper matching ui-v2 PageLayout API"
      exports: ["PageLayout"]
    - path: "src/ds/composites/Section.tsx"
      provides: "Content grouping section"
      exports: ["Section"]
    - path: "src/ds/primitives/LinkButton.tsx"
      provides: "Next.js Link + Button wrapper"
      exports: ["LinkButton"]
    - path: "src/lib/format.ts"
      provides: "formatCurrency, formatBytes utilities moved from ui-v2"
      exports: ["formatCurrency"]
  key_links:
    - from: "all 188 migrated files"
      to: "@/ds"
      via: "import statements"
      pattern: "from '@/ds'"
    - from: "src/ds/composites/PageLayout.tsx"
      to: "src/ds/composites/PageHeader.tsx"
      via: "composition"
      pattern: "import.*PageHeader"
---

<objective>
Migrate all 188 files importing from @/components/ui-v2 and 5 files importing from @/components/ui/ to use @/ds equivalents. Build gap components that don't yet exist in ds/. After migration, delete both legacy directories. Update CLAUDE.md documentation.

Purpose: Complete the design system unification so the codebase has a single component system (ds/) with no legacy imports remaining.
Output: Zero legacy imports, deleted ui/ and ui-v2/ directories, updated documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modes-cleanup/04-CONTEXT.md
@.planning/phases/04-modes-cleanup/04-RESEARCH.md

@src/ds/index.ts
@src/ds/composites/index.ts
@src/ds/primitives/index.ts

<interfaces>
<!-- Component mapping reference for migration -->

DIRECT 1:1 MAPPINGS (import swap only):
  ui-v2 forms/Button       -> Button from @/ds
  ui-v2 feedback/Alert      -> Alert from @/ds (use 'tone' not 'variant')
  ui-v2 forms/Input         -> Input from @/ds
  ui-v2 forms/Select        -> Select from @/ds
  ui-v2 forms/Textarea      -> Textarea from @/ds
  ui-v2 forms/Checkbox      -> Checkbox from @/ds
  ui-v2 forms/Radio         -> Radio from @/ds
  ui-v2 forms/Toggle        -> Switch from @/ds (NAME CHANGE)
  ui-v2 forms/SearchInput   -> SearchInput from @/ds
  ui-v2 display/Badge       -> Badge from @/ds (use 'tone' not 'variant')
  ui-v2 display/EmptyState  -> Empty from @/ds (NAME CHANGE)
  ui-v2 display/Stat        -> Stat from @/ds
  ui-v2 feedback/Spinner    -> Spinner from @/ds
  ui-v2 feedback/Skeleton   -> Skeleton from @/ds
  ui-v2 feedback/ProgressBar -> ProgressBar from @/ds
  ui-v2 feedback/Toast      -> toast from @/ds
  ui-v2 overlay/Modal       -> Modal from @/ds
  ui-v2 overlay/ConfirmDialog -> ConfirmDialog from @/ds (use 'message' not 'description')
  ui-v2 overlay/Drawer      -> Drawer from @/ds
  ui-v2 overlay/Popover     -> Popover from @/ds
  ui-v2 navigation/Tabs     -> Tabs from @/ds
  ui-v2 navigation/Dropdown -> Dropdown from @/ds

STRUCTURAL CHANGES (JSX restructuring needed):
  ui-v2 layout/Card -> Card + CardHeader + CardBody + CardFooter from @/ds
    - OLD: <Card title="X" subtitle="Y">content</Card>
    - NEW: <Card><CardHeader title="X" subtitle="Y" /><CardBody>content</CardBody></Card>
    - CardHeader requires 'title' prop (not children)

API DIFFERENCES:
  ui-v2 forms/FormGroup -> Field from @/ds
    - FormGroup props: label, htmlFor, required, helpText, error, children
    - Field props:     label, required, hint (was helpText), error, children
    - Drop htmlFor (Field uses useId internally)
    - Rename helpText -> hint

  ui-v2 navigation/TabNav -> SectionNav from @/ds
    - Verify pill-strip API compatibility

  ui-v2 navigation/Pagination -> TablePagination from @/ds (or new standalone Pagination)

GAP COMPONENTS (build in ds/ before migration):
  PageLayout   -> new ds/composites/PageLayout.tsx (81 files depend on this)
  Section      -> new ds/composites/Section.tsx (41 files)
  LinkButton   -> new ds/primitives/LinkButton.tsx (20 files)
  DataTable    -> new ds/composites/DataTable.tsx (24 files) OR inline to Table
  Container    -> inline as div with max-width (3 files)
  FilterPanel  -> inline (3 files, domain-specific)
  BackButton   -> inline as Button + router.back() (2 files)
  DebouncedTextarea -> inline with useDebounce (2 files)
  Accordion    -> new ds/primitives/Accordion.tsx (1 file)
  Form/FormActions -> inline as <form> + footer div (9 files)
  HeaderNav    -> fold into PageLayout as navItems prop

UTILITY MOVES:
  ui-v2/utils/format.ts -> src/lib/format.ts (formatCurrency, formatBytes, etc.)
  ui-v2/refunds/RefundDialog -> src/components/features/invoices/RefundDialog.tsx
  ui-v2/refunds/RefundHistoryTable -> src/components/features/invoices/RefundHistoryTable.tsx

SortableHeader (from ui/):
  src/components/ui/SortableHeader.tsx -> inline into consuming files or build sort support into ds/ Table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build gap components and move utilities</name>
  <files>
    src/ds/composites/PageLayout.tsx,
    src/ds/composites/Section.tsx,
    src/ds/composites/DataTable.tsx,
    src/ds/primitives/LinkButton.tsx,
    src/ds/primitives/Accordion.tsx,
    src/ds/primitives/Pagination.tsx,
    src/ds/composites/index.ts,
    src/ds/primitives/index.ts,
    src/lib/format.ts,
    src/components/features/invoices/RefundDialog.tsx,
    src/components/features/invoices/RefundHistoryTable.tsx
  </files>
  <action>
Build all missing ds/ components needed before bulk migration can proceed. These are blockers -- without them, 81+ files cannot be migrated.

**1. PageLayout composite (src/ds/composites/PageLayout.tsx) -- blocks 81 files:**

Match the ui-v2 PageLayout API as closely as possible to minimize migration friction. This is a `'use client'` component.

```typescript
interface PageLayoutProps {
  title: string
  subtitle?: string
  breadcrumbs?: Array<{ label: string; href?: string }>
  backButton?: { label: string; href?: string; onBack?: () => void }
  headerActions?: React.ReactNode
  showHeaderActionsOnMobile?: boolean
  compactHeader?: boolean
  navItems?: Array<{ label: string; href: string; active?: boolean; count?: number }>
  loading?: boolean
  error?: string
  children: React.ReactNode
  className?: string
}
```

Implementation:
- Compose with existing `PageHeader` from `@/ds/composites` for the title/breadcrumbs/actions area.
- If `navItems` provided, render `SectionNav` from `@/ds/composites` below the header.
- If `loading` is true, show a centered `Spinner`.
- If `error` is set, show an `Alert` with tone="danger".
- Wrap children in a simple div with optional className.
- If `backButton` provided, render a back link/button in the header area using the ds/ Button with variant="ghost" and an arrow-left Icon.
- No Container wrapper needed -- the AppShell main area already handles padding.

**2. Section composite (src/ds/composites/Section.tsx) -- blocks 41 files:**

```typescript
interface SectionProps {
  title?: string
  description?: string
  actions?: React.ReactNode
  collapsible?: boolean
  defaultOpen?: boolean
  children: React.ReactNode
  className?: string
}
```

Implementation:
- Title in a `text-base font-semibold text-text` heading.
- Description in `text-sm text-text-muted` below title.
- Actions aligned right of title via flex.
- If `collapsible`, use Headless UI `Disclosure` component for expand/collapse with chevron icon.
- Children rendered in a div below the header with `mt-4` spacing.

**3. LinkButton primitive (src/ds/primitives/LinkButton.tsx) -- blocks 20 files:**

```typescript
interface LinkButtonProps {
  href: string
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger'
  size?: 'sm' | 'md' | 'lg'
  icon?: React.ReactNode
  children: React.ReactNode
  className?: string
}
```

Implementation: Wraps Next.js `Link` component styled like ds/ `Button`. Import the button style utility or re-use Tailwind classes matching Button's variant/size tokens. Do NOT render `<Button>` inside `<Link>` -- render `<Link>` with button classes directly.

**4. DataTable composite (src/ds/composites/DataTable.tsx) -- blocks 24 files:**

A data-driven table wrapper around the declarative ds/ Table.

```typescript
interface DataTableColumn<T> {
  key: string
  header: string
  render?: (value: unknown, row: T) => React.ReactNode
  sortable?: boolean
  className?: string
}

interface DataTableProps<T> {
  columns: DataTableColumn<T>[]
  data: T[]
  keyField?: string
  emptyMessage?: string
  onRowClick?: (row: T) => void
  sortColumn?: string
  sortDirection?: 'asc' | 'desc'
  onSort?: (column: string) => void
  pagination?: {
    page: number
    pageSize: number
    total: number
    onPageChange: (page: number) => void
  }
  className?: string
}
```

Implementation: Maps data-driven API to `<Table>`, `<TableHeader>`, `<TableBody>`, `<TableRow>`, `<TableHead>`, `<TableCell>` from ds/. Renders `<TablePagination>` footer when pagination prop provided. Renders `<Empty>` when data is empty. Sort headers use clickable `<TableHead>` with chevron icons.

**5. Accordion primitive (src/ds/primitives/Accordion.tsx) -- 1 file:**

Use Headless UI `Disclosure` component. Simple expand/collapse with title and chevron.

```typescript
interface AccordionProps {
  items: Array<{ title: string; content: React.ReactNode; defaultOpen?: boolean }>
  className?: string
}
```

**6. Pagination primitive (src/ds/primitives/Pagination.tsx) -- 9 files:**

Standalone pagination (not tied to Table) for files that use Pagination outside of DataTable context.

```typescript
interface PaginationProps {
  page: number
  totalPages: number
  onPageChange: (page: number) => void
  className?: string
}
```

Renders prev/next buttons and page numbers. Uses ds/ Button variant="ghost" for controls.

**7. Move format utilities to src/lib/format.ts:**

Copy `formatCurrency`, `formatBytes`, `formatNumber`, `formatPercentage`, and any other exports from `src/components/ui-v2/utils/format.ts` to `src/lib/format.ts`. Keep the same function signatures. Add re-exports if the file already exists (merge, don't overwrite).

**8. Move refund components:**

Copy `src/components/ui-v2/refunds/RefundDialog.tsx` to `src/components/features/invoices/RefundDialog.tsx`. Copy `src/components/ui-v2/refunds/RefundHistoryTable.tsx` to `src/components/features/invoices/RefundHistoryTable.tsx`. Update their internal imports from ui-v2 to ds/.

**9. Update barrel exports:**

Add all new components to `src/ds/composites/index.ts` and `src/ds/primitives/index.ts` so they're available via `import { PageLayout, Section, ... } from '@/ds'`.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- PageLayout, Section, LinkButton, DataTable, Accordion, Pagination exist in ds/ and export from barrel
- src/lib/format.ts contains formatCurrency and other format utilities
- RefundDialog and RefundHistoryTable moved to src/components/features/invoices/ with ds/ imports
- All new components type-check cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 193 files from ui-v2 and ui/ to ds/ imports</name>
  <files>All 188 ui-v2 files + 5 ui/ files listed in files_modified above</files>
  <action>
Migrate every file that imports from `@/components/ui-v2` or `@/components/ui/` to use `@/ds` equivalents. Process files area by area in this order. For EACH file:

1. Replace all `@/components/ui-v2/*` imports with `@/ds` barrel imports
2. Replace all `@/components/ui/SortableHeader` imports with inline implementation or ds/ equivalent
3. Apply the component mapping rules from the interfaces block above
4. Handle structural changes (Card subcomponents, FormGroup->Field rename, etc.)

**Migration pattern for each file:**

```typescript
// BEFORE (typical):
import { Button } from '@/components/ui-v2/forms/Button'
import { Card } from '@/components/ui-v2/layout/Card'
import { PageLayout } from '@/components/ui-v2/layout/PageLayout'
import { FormGroup } from '@/components/ui-v2/forms/FormGroup'
import { Alert } from '@/components/ui-v2/feedback/Alert'
import { Badge } from '@/components/ui-v2/display/Badge'
import { toast } from '@/components/ui-v2/feedback/Toast'

// AFTER:
import { Button, Card, CardHeader, CardBody, PageLayout, Field, Alert, Badge, toast } from '@/ds'
```

**Area-by-area order and special notes:**

**Wave 1 -- Settings (28 files):** Repetitive pattern -- most use PageLayout + Card + Button + FormGroup + Input + Select + Toast. Apply the standard mapping. FormGroup -> Field (rename helpText to hint).

**Wave 2 -- Private Bookings (14 files):** Uses DataTable, LinkButton, Modal, Badge extensively. Calendar page may use custom calendar components -- preserve those, only swap ui-v2 primitives. PaymentHistoryTable uses DataTable -- swap to new ds/ DataTable.

**Wave 3 -- Employees (5 files):** EmployeeForm and detail pages use many FormGroup instances. Section component used for grouping. Swap FormGroup -> Field, Section -> ds/ Section.

**Wave 4 -- Invoices + Quotes (18 files):** Uses Pagination, DataTable, LinkButton. Some files use the format utility (formatCurrency) -- update to import from `@/lib/format`. Recurring invoices pages have complex forms.

**Wave 5 -- Rota (15 files):** Multiple modals (AddShiftsModal, BookHolidayModal, CreateShiftModal, ShiftDetailModal). Each modal uses Modal + FormGroup + Input + Select. RotaGrid uses complex custom rendering -- only swap the ui-v2 primitives it imports.

**Wave 6 -- Menu Management (16 files):** Uses Drawer heavily (DishDrawer, IngredientDrawer, RecipeDrawer). Expandable rows use custom patterns. Only swap the ui-v2 imports.

**Wave 7 -- Receipts (8 files):** Uses Section, Card, Badge, Stat, DataTable. MonthlyCharts likely has chart-specific code -- only swap ui-v2 primitives.

**Wave 8 -- Remaining authenticated areas (29 files):**
- Customers (3): PageLayout, Card, FormGroup, DataTable
- Dashboard (1): Card/Badge only (ScheduleCalendar)
- Expenses insights (2): Badge, Stat, ui/ SortableHeader -- replace SortableHeader with sortable TableHead from ds/
- Messages bulk (1): Button, Input, Textarea
- MGD insights (2): Badge, Stat
- Mileage (3): PageLayout, Badge, Stat, ui/ SortableHeader -- same SortableHeader replacement
- Parking (1): PageLayout, Card, DataTable
- Profile (1): Card, FormGroup, Input, Button
- Roles (4): PageLayout, Card, Badge, Modal
- Table Bookings BOH/FOH/reports (9): PageLayout, Card, Modal, Badge, LinkButton, ConfirmDialog. FOH files also use FohClockWidget which has ui-v2 Modal -- migrate that too.

**Wave 9 -- Auth + Staff Portal + Public (6 files):**
- Login (page-client.tsx): Uses Container, Card, Form, FormActions, FormGroup, Input, Button, LinkButton, toast. Replace Container with a centered div (`max-w-md mx-auto`). Replace Form with `<form>`. Replace FormActions with a flex div. Rest are standard mappings.
- Reset password (2 files): Similar to login.
- Staff Portal leave (2 files): Badge, Button, Input, FormGroup, Alert.
- Booking portal (1 file): formatCurrency only -- update import to `@/lib/format`.

**Wave 10 -- Shared components (43 files):**
- Events command center (5): Card, Button, Badge, Alert, Input, Select, FilterPanel. Inline FilterPanel as custom div with filter inputs.
- Features/catering (3): Card, Button, Modal, Input, FormGroup, Alert, toast.
- Features/customers (3): FormGroup, Input, Select, Button, Alert.
- Features/employees (14): Heavy FormGroup/Input/Select usage. Systematic FormGroup->Field swap.
- Features/events (6): Card, Button, Badge, FormGroup, Input, Select.
- Features/invoices (1): Modal, Button, Input.
- Features/menu (1): Modal, Button, Input.
- Features/shared (2): AppNavigation may import nav items -- check if it uses ui-v2 types. SquareImageUpload uses ui-v2 components.
- Modals (3): Modal, Button, Input, FormGroup.
- Private bookings shared (4): CalendarView, CommunicationsTab, DeleteBookingButton, SmsQueueActionForm.
- Schedule calendar (1): Card, Badge.

**Wave 11 -- SortableHeader files (5 files from ui/):**
Replace `import { SortableHeader } from '@/components/ui/SortableHeader'` with either:
- Inline the sortable header as a `<button>` with sort icon inside `<TableHead>`, OR
- Build a tiny `SortableTableHead` wrapper that uses ds/ TableHead + Icon.
The SortableHeader component is a button that toggles sort direction and renders an arrow icon. Inline this pattern directly in each consuming file using ds/ components (Button variant="ghost" or a plain clickable th).

**Critical rules for ALL migrations:**
- Do NOT change any business logic, data fetching, or event handlers
- Do NOT change file names or directory structure
- Do NOT remove or add any functionality
- ONLY change imports and component usage to match ds/ APIs
- If a component uses `variant="info"` for Alert/Badge, check if ds/ uses `tone="info"` instead
- If a component uses `<ConfirmDialog description="...">`, change to `message="..."`
- If a component uses `<Tabs items={...}>`, check ds/ uses `tabs` prop instead
- Card structural changes: flatten `<Card title="X">content</Card>` into `<Card><CardHeader title="X" /><CardBody>content</CardBody></Card>`
- Toast: if importing the toast function, import as `import { toast } from '@/ds'`

**After all files migrated, verify:**
```bash
grep -rl "@/components/ui-v2" src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules"
grep -rl "@/components/ui/" src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v "ui-v2"
```
Both commands must return zero results.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && grep -rl "@/components/ui-v2" src/ --include="*.tsx" --include="*.ts" | wc -l && grep -rl "@/components/ui/" src/ --include="*.tsx" --include="*.ts" | grep -v "ui-v2" | wc -l && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
- grep for @/components/ui-v2 returns 0 files
- grep for @/components/ui/ returns 0 files
- TypeScript compiles with zero errors
- All 193 files migrated to @/ds imports
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete legacy directories and update documentation</name>
  <files>
    src/components/ui-v2/ (DELETE),
    src/components/ui/ (DELETE),
    CLAUDE.md
  </files>
  <action>
**1. Final pre-deletion verification:**

Run these checks and confirm zero results before deleting anything:
```bash
grep -rl "@/components/ui-v2" src/ --include="*.tsx" --include="*.ts"
grep -rl "@/components/ui/" src/ --include="*.tsx" --include="*.ts" | grep -v "ui-v2"
```

If ANY results remain, fix those files first. Do NOT proceed with deletion until both return zero.

**2. Delete legacy directories:**

```bash
rm -rf src/components/ui-v2/
rm -rf src/components/ui/
```

**3. Verify build after deletion:**

```bash
npm run build
```

This is the critical test. If the build fails, it means a file still references a deleted component. Fix any failures by finding the broken import and updating it to @/ds.

**4. Update CLAUDE.md documentation (per CLEAN-04):**

Find and update these specific sections in the project-level CLAUDE.md:

a) Find the line "Migrating from legacy `PageWrapper`/`Page` pattern to `PageLayout` + `HeaderNav` from `src/components/ui-v2/`" and replace with:
   "All pages use `@/ds` design system components (`PageLayout`, `PageHeader`, `Card`, etc.)"

b) Find "New code uses `src/components/ui-v2/` via the barrel export `src/components/ui-v2/index.ts`" and replace with:
   "All components imported from `@/ds` (barrel: `src/ds/index.ts`)"

c) Find "Legacy `PageWrapper`/`Page` pattern still exists but being phased out" and DELETE this line entirely.

d) Find "New pages: use `PageLayout` + `HeaderNav` from `ui-v2`" and replace with:
   "All pages use `PageLayout` + `PageHeader` from `@/ds`"

e) Find "Defined in `src/components/ui-v2/tokens.ts`" and replace with:
   "Design tokens defined in `globals.css` `@theme` block, JS access via `@/ds` tokens"

f) Find "`src/components/ui-v2/index.ts` is the single export point for the UI library" and replace with:
   "`src/ds/index.ts` is the single export point for the design system"

g) Find "`src/components/ui-v2/navigation/index.ts` sub-barrel for navigation components" and DELETE this line.

h) In the "UI Components" section under Architecture, update to reflect the completed migration state. Remove any migration-in-progress language.

i) Search for any other "ui-v2" references in CLAUDE.md and update or remove them.

**5. Clean up components.json (optional, per RESEARCH):**

If `components.json` exists and references `tailwind.config.ts`, update or remove the stale reference.

**6. Sweep inline comments:**

Run a quick grep for comments referencing ui-v2 migration in src/ files:
```bash
grep -rn "ui-v2" src/ --include="*.tsx" --include="*.ts" | grep -i "todo\|migrate\|legacy\|fixme\|hack"
```

Update or remove any outdated migration comments found.

**7. Final verification:**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

All three must pass with zero errors and zero warnings.
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && test ! -d src/components/ui-v2 && test ! -d src/components/ui && npm run build 2>&1 | tail -5 && echo "PASS: directories deleted, build succeeds"</automated>
  </verify>
  <done>
- src/components/ui-v2/ directory does not exist
- src/components/ui/ directory does not exist
- npm run build succeeds
- npm run lint passes with zero warnings
- CLAUDE.md references @/ds as canonical component system with no ui-v2 mentions
- CLEAN-01, CLEAN-02, CLEAN-03 (already satisfied), CLEAN-04 all complete
  </done>
</task>

</tasks>

<verification>
1. `grep -rl "@/components/ui-v2" src/ --include="*.tsx" --include="*.ts"` -- returns zero results
2. `grep -rl "@/components/ui/" src/ --include="*.tsx" --include="*.ts" | grep -v "ui-v2"` -- returns zero results
3. `test ! -d src/components/ui-v2 && echo "DELETED"` -- prints DELETED
4. `test ! -d src/components/ui && echo "DELETED"` -- prints DELETED
5. `npm run build` -- succeeds
6. `npm run lint` -- zero warnings
7. `npx tsc --noEmit` -- zero errors
8. `grep "ds/" CLAUDE.md` -- returns updated documentation lines
9. `grep "ui-v2" CLAUDE.md` -- returns zero results
</verification>

<success_criteria>
- All 193 files migrated from ui-v2/ui imports to @/ds imports
- Six new ds/ components available: PageLayout, Section, LinkButton, DataTable, Accordion, Pagination
- Format utilities available at @/lib/format.ts
- Both legacy directories deleted
- Build, lint, and type-check all pass
- CLAUDE.md documents @/ds as canonical component system
</success_criteria>

<output>
After completion, create `.planning/phases/04-modes-cleanup/04-02-SUMMARY.md`
</output>
