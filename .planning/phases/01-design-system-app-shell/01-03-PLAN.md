---
phase: 01-design-system-app-shell
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/ds/composites/Card.tsx
  - src/ds/composites/PageHeader.tsx
  - src/ds/composites/SectionNav.tsx
  - src/ds/composites/Tabs.tsx
  - src/ds/composites/Segmented.tsx
  - src/ds/composites/Table.tsx
  - src/ds/composites/index.ts
  - src/ds/icons/Icon.tsx
  - src/ds/icons/paths.ts
  - src/ds/icons/index.ts
  - src/ds/index.ts
autonomous: true
requirements: [DS-05, DS-08, DS-09, DS-13, DS-15, DS-16, DS-20]

must_haves:
  truths:
    - "Card renders with header (title/subtitle/action), padded body, and optional footer"
    - "PageHeader renders breadcrumbs with chevron separators, title, subtitle, and action buttons"
    - "SectionNav renders horizontal pill strip with optional counts on each item"
    - "Tabs renders underline-style tabs with count pills and controlled active state"
    - "Segmented control renders inline button group with sliding active highlight"
    - "Table renders with styled header/body/row/cell, hover states, sortable columns, and pagination footer"
    - "Icon component renders 38+ SVG icons at 16px on 24x24 viewBox with strokeWidth 1.75"
    - "All composites use @theme tokens only -- no hardcoded hex colours"
  artifacts:
    - path: "src/ds/composites/Card.tsx"
      provides: "Card with header, body, footer"
      exports: ["Card", "CardHeader", "CardBody", "CardFooter"]
    - path: "src/ds/composites/Table.tsx"
      provides: "Table wrapper with styled cells and pagination"
      exports: ["Table", "TableHeader", "TableBody", "TableRow", "TableHead", "TableCell", "TablePagination"]
    - path: "src/ds/icons/Icon.tsx"
      provides: "SVG icon component"
      exports: ["Icon"]
    - path: "src/ds/icons/paths.ts"
      provides: "38+ icon SVG path definitions"
      exports: ["iconPaths"]
    - path: "src/ds/composites/index.ts"
      provides: "Barrel re-export of all composites"
      exports: ["Card", "PageHeader", "SectionNav", "Tabs", "Segmented", "Table"]
  key_links:
    - from: "src/ds/icons/Icon.tsx"
      to: "src/ds/icons/paths.ts"
      via: "imports iconPaths for SVG rendering"
      pattern: "import.*iconPaths.*from.*paths"
    - from: "src/ds/composites/PageHeader.tsx"
      to: "src/ds/icons/Icon.tsx"
      via: "chevron separator icon in breadcrumbs"
      pattern: "import.*Icon.*from.*@/ds/icons"
    - from: "src/ds/index.ts"
      to: "src/ds/composites/index.ts"
      via: "barrel re-export"
      pattern: "export.*from.*composites"
---

<objective>
Build all 7 composite components and the complete 38+ icon set. Composites build on primitives and provide higher-level UI patterns (cards, tables, navigation). The icon set provides the custom SVG icons specified in the design handoff.

Purpose: Composites are used by every screen in Phase 2. The Table, Card, PageHeader, and Tabs composites are the most-used patterns across all 28 screens. The icon set replaces reliance on Lucide/Heroicons in new code.

Output: 7 composite components + Icon component with 38+ named icons, all exported via barrel files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-app-shell/01-CONTEXT.md
@.planning/phases/01-design-system-app-shell/01-RESEARCH.md
@.planning/phases/01-design-system-app-shell/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01-01 outputs needed by this plan -->

From src/app/globals.css @theme block:
  All --color-*, --spacing-*, --radius-*, --shadow-* tokens available as Tailwind utilities
  e.g., bg-primary, text-text-muted, rounded-default, shadow-sm, border-border

From src/lib/utils.ts:
  export function cn(...inputs: ClassValue[]): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Icon component with 38+ SVG icon paths from design handoff</name>
  <files>src/ds/icons/Icon.tsx, src/ds/icons/paths.ts, src/ds/icons/index.ts</files>
  <read_first>
    - .planning/phases/01-design-system-app-shell/01-RESEARCH.md (Code Examples section has Icon component reference implementation and icon path data from handoff icons.jsx)
    - src/app/globals.css (verify @theme tokens exist from Plan 01-01)
  </read_first>
  <action>
    Build the Icon system in 3 files:

    **Icon.tsx** (server component -- no 'use client'):
    Reference implementation from 01-RESEARCH.md Code Examples section. Exact spec:
    - Props: name (IconName -- keyof typeof iconPaths), size (number, default 16), className (string, optional), style (CSSProperties, optional)
    - Renders SVG: width={size}, height={size}, viewBox="0 0 24 24", fill="none", stroke="currentColor", strokeWidth={1.75}, strokeLinecap="round", strokeLinejoin="round"
    - style includes flexShrink: 0, display: 'inline-block'
    - Returns null if iconPaths[name] is undefined

    **paths.ts**:
    Export a const object `iconPaths` mapping icon names to JSX SVG path elements. Each icon is one or more <path>, <circle>, <line>, <polyline>, or <rect> elements.

    The 38+ icons needed (names from the design handoff nav.jsx and component specs):
    home, calendar, mic, users, message, grid, table, building, truck, user, clock,
    cash, file, briefcase, receipt, map, pound, trendUp, link, cog, palette,
    search, bell, plus, chevronRight, chevronDown, chevronLeft, chevronUp,
    x, check, alertCircle, alertTriangle, info, arrowUp, arrowDown,
    filter, download, upload, edit, trash

    For each icon, create clean stroke-based SVG paths on 24x24 coordinate space. Use standard geometric stroke-icon conventions (similar to Lucide but matching the design handoff spec of strokeWidth 1.75):

    Example entries:
    ```typescript
    import React from 'react'

    export const iconPaths = {
      home: (
        <>
          <path d="M3 9.5L12 3l9 6.5V20a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V9.5z" />
          <polyline points="9 22 9 12 15 12 15 22" />
        </>
      ),
      calendar: (
        <>
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2" />
          <line x1="16" y1="2" x2="16" y2="6" />
          <line x1="8" y1="2" x2="8" y2="6" />
          <line x1="3" y1="10" x2="21" y2="10" />
        </>
      ),
      // ... all 38+ icons
    } as const
    ```

    IMPORTANT: Each icon should be visually recognizable at 16px rendered size. Use consistent 1.75 strokeWidth. Paths must be valid SVG.

    **index.ts** barrel for icons:
    ```typescript
    export { Icon } from './Icon'
    export type { IconName } from './Icon'
    // Re-export for consumers that want to check available icons
    export { iconPaths } from './paths'
    ```
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/ds/icons/Icon.tsx exports "Icon" with name prop typed as keyof typeof iconPaths
    - src/ds/icons/Icon.tsx contains "viewBox" and "0 0 24 24" and "strokeWidth" and "1.75"
    - src/ds/icons/Icon.tsx does NOT contain "'use client'"
    - src/ds/icons/paths.ts exports "iconPaths" containing at least keys: "home", "calendar", "users", "search", "bell", "plus", "chevronRight", "x", "check"
    - src/ds/icons/paths.ts contains at least 38 icon entries
    - src/ds/icons/index.ts contains "export { Icon }" and "export { iconPaths }"
    - NO file under src/ds/icons/ imports from "@/components/ui-v2" or "lucide-react" or "@heroicons/react"
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Icon component with 38+ named SVG icons, all rendering at 16px on 24x24 viewBox with strokeWidth 1.75</done>
</task>

<task type="auto">
  <name>Task 2: Build Card, PageHeader, SectionNav, Tabs, Segmented, Table composites and barrel exports</name>
  <files>
    src/ds/composites/Card.tsx, src/ds/composites/PageHeader.tsx, src/ds/composites/SectionNav.tsx,
    src/ds/composites/Tabs.tsx, src/ds/composites/Segmented.tsx, src/ds/composites/Table.tsx,
    src/ds/composites/index.ts, src/ds/index.ts
  </files>
  <read_first>
    - src/ds/icons/Icon.tsx (from Task 1 -- use for chevrons in PageHeader breadcrumbs)
    - src/ds/primitives/Badge.tsx (if Tabs needs count badge pattern reference)
    - .planning/phases/01-design-system-app-shell/01-RESEARCH.md (component specs for each composite)
    - src/lib/utils.ts (cn function)
  </read_first>
  <action>
    Build 6 composite components + barrel exports:

    **Card.tsx** (server component -- no 'use client'):
    - Compound component pattern: Card, CardHeader, CardBody, CardFooter
    - Card: bg-surface border border-border rounded-lg shadow-sm overflow-hidden
    - CardHeader: px-[var(--spacing-pad-card)] py-3 border-b border-border flex items-center justify-between
      Props: title (string), subtitle (string, optional), action (ReactNode, optional)
    - CardBody: p-[var(--spacing-pad-card)]
    - CardFooter: px-[var(--spacing-pad-card)] py-3 border-t border-border bg-surface-2
    - All sub-components accept className for extension

    **PageHeader.tsx** (server component):
    - Props: breadcrumbs ({label:string, href?:string}[], optional), title (string), subtitle (string, optional), actions (ReactNode, optional)
    - Breadcrumbs: flex items-center gap-1 text-sm text-text-muted mb-2
      Each crumb: if href, render as anchor text-text-muted hover:text-text; if last, text-text font-medium
      Separator: Icon name="chevronRight" size={14} between each crumb (from src/ds/icons)
    - Title: text-2xl font-bold text-text-strong tracking-tight
    - Subtitle: text-sm text-text-muted mt-1
    - Actions: flex items-center gap-2, positioned on the right (flex justify-between on the title row)
    - Spacing: pb-6 border-b border-border mb-6

    **SectionNav.tsx** ('use client' -- needs active state):
    - Props: items ({id:string, label:string, count?:number, href?:string}[]), activeId (string), onSelect ((id:string)=>void, optional)
    - Layout: flex items-center gap-1 overflow-x-auto scrollbar-hide
    - Each item: px-3 py-1.5 text-[13px] font-medium rounded-pill whitespace-nowrap transition-colors
    - Active item: bg-primary-soft text-primary-soft-fg
    - Inactive: text-text-muted hover:bg-surface-hover hover:text-text
    - Count: inline Badge-like span after label text, text-xs bg-surface-2 rounded-pill px-1.5 min-w-5 text-center
    - If href provided, render as Link; if onSelect provided, render as button

    **Tabs.tsx** ('use client'):
    - Props: tabs ({id:string, label:string, count?:number}[]), activeTab (string), onTabChange ((id:string)=>void)
    - Layout: flex items-center border-b border-border
    - Each tab button: px-4 py-2.5 text-[13px] font-medium whitespace-nowrap relative
    - Active tab: text-primary, with a 2px absolute bottom border (bg-primary, h-0.5, inset-x-0, bottom-0, rounded-full)
    - Inactive: text-text-muted hover:text-text
    - Count pill: ml-2 text-xs bg-surface-2 text-text-muted rounded-pill px-1.5 inline-flex items-center min-w-5 justify-center
    - Active count: bg-primary-soft text-primary-soft-fg

    **Segmented.tsx** ('use client'):
    - Props: options ({id:string, label:string}[]), value (string), onChange ((id:string)=>void), size ('sm'|'md', default 'md')
    - Container: inline-flex bg-surface-2 p-0.5 rounded-default border border-border
    - Each option button: px-3 py-1 text-[13px] font-medium rounded-[6px] transition-all duration-150
    - Active: bg-surface text-text shadow-sm
    - Inactive: text-text-muted hover:text-text
    - The sliding active indicator uses bg-surface shadow-sm on the active button (no CSS ::before sliding element needed -- just style the active button with background + shadow)

    **Table.tsx** (server component for wrapper; cell components are server components):
    - Compound component pattern: Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TablePagination
    - Table: w-full wrapper div with overflow-x-auto, inner <table> with w-full border-collapse
    - TableHeader: <thead> with bg-surface-2
    - TableHead: <th> px-4 py-2 text-left text-xs font-medium text-text-muted uppercase tracking-wider
      Optional sortable prop: renders sort icon (chevronUp/chevronDown based on direction), onClick handler
    - TableBody: <tbody> divide-y divide-border
    - TableRow: <tr> hover:bg-surface-hover transition-colors
    - TableCell: <td> px-4 py-[var(--spacing-row-h)] text-[13px] text-text whitespace-nowrap
    - TablePagination ('use client'): flex items-center justify-between px-4 py-3 border-t border-border
      Props: page (number), totalPages (number), onPageChange ((page:number)=>void), pageSize (number, optional), totalItems (number, optional)
      Shows: "Showing X-Y of Z" on left, page buttons on right
      Note: TableHead with sortable also needs 'use client' -- extract as a separate SortableTableHead client component

    **composites/index.ts** barrel:
    ```typescript
    export { Card, CardHeader, CardBody, CardFooter } from './Card'
    export { PageHeader } from './PageHeader'
    export { SectionNav } from './SectionNav'
    export { Tabs } from './Tabs'
    export { Segmented } from './Segmented'
    export { Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TablePagination } from './Table'
    ```

    **Update src/ds/index.ts** to include all sub-modules:
    ```typescript
    export * from './primitives'
    export * from './composites'
    export * from './icons'
    export * from './tokens'
    ```
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-AnchorManagementTools && npx tsc --noEmit 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/ds/composites/Card.tsx exports "Card", "CardHeader", "CardBody", "CardFooter"
    - src/ds/composites/PageHeader.tsx exports "PageHeader" and imports Icon from "@/ds/icons"
    - src/ds/composites/SectionNav.tsx exports "SectionNav" and contains "'use client'"
    - src/ds/composites/Tabs.tsx exports "Tabs" and contains "'use client'" and has "border-b" in className
    - src/ds/composites/Segmented.tsx exports "Segmented" and contains "'use client'"
    - src/ds/composites/Table.tsx exports "Table", "TableHeader", "TableBody", "TableRow", "TableHead", "TableCell"
    - src/ds/composites/index.ts contains re-exports for all 6 composites
    - src/ds/index.ts contains "export * from './primitives'" and "export * from './composites'" and "export * from './icons'"
    - NO file under src/ds/composites/ imports from "@/components/ui-v2"
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>6 composite components + barrel exports complete, all using @theme tokens, Icon integrated where needed, full ds/ barrel works</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` succeeds
2. `npm run build` succeeds
3. `npm run lint` succeeds (zero warnings)
4. `ls src/ds/composites/` shows 7 files (6 components + index.ts)
5. `ls src/ds/icons/` shows 3 files (Icon.tsx, paths.ts, index.ts)
6. `grep -c "export" src/ds/icons/paths.ts` confirms iconPaths exported
7. `grep -r "ui-v2" src/ds/` returns zero results
8. Test import works: `import { Card, Table, Icon, Tabs } from '@/ds'` compiles
</verification>

<success_criteria>
- 7 composite components (Card, PageHeader, SectionNav, Tabs, Segmented, Table with sub-components) built and exported
- Icon component renders 38+ named SVG icons at correct spec (16px, 24x24 viewBox, 1.75 strokeWidth)
- All composites use @theme tokens, no hardcoded hex
- PageHeader uses Icon for breadcrumb chevrons
- Table supports sortable columns and pagination footer
- Full barrel export: `import { Card, Table, Icon, PageHeader, Tabs, Button, Badge } from '@/ds'` works
- Zero type errors, zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-app-shell/01-03-SUMMARY.md`
</output>
